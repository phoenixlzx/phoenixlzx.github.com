<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title><![CDATA[Phoenix's island]]></title>
  <subtitle><![CDATA[Sun will shine on the horizon.]]></subtitle>
  <link href="/atom.xml" rel="self"/>
  <link href="http://blog.phoenixlzx.com/"/>
  <updated>2014-11-12T03:09:11.072Z</updated>
  <id>http://blog.phoenixlzx.com/</id>
  
  <author>
    <name><![CDATA[Phoenix Nemo]]></name>
    <email><![CDATA[i@phoenixlzx.com]]></email>
  </author>
  
  <generator uri="http://zespia.tw/hexo/">Hexo</generator>
  
  <entry>
    <title><![CDATA[用优雅的方式在 OS X 中为单个应用设置语言]]></title>
    <link href="http://blog.phoenixlzx.com/2014/11/11/set-language-for-individual-app-in-os-x-the-elegant-way/"/>
    <id>http://blog.phoenixlzx.com/2014/11/11/set-language-for-individual-app-in-os-x-the-elegant-way/</id>
    <published>2014-11-10T23:42:27.000Z</published>
    <updated>2014-11-11T00:35:53.000Z</updated>
    <content type="html"><![CDATA[<p>买到 CLIP STUDIO PAINT Pro，激活之后发现不给启动，显示 <code>Unsupported OS</code> 并退出。搜索了下发现是因为语言设置的问题导致，需要将系统环境设置为应用所支持的语言才能运行。</p>
<p>嘛。日本人做事情也是让人无话可说，那么多应用都没有多语言支持的… 用能支持的语言来显示就好了嘛。</p>
<p>话说回来，我一开始设置的系统语言是简体中文，虽然后备语言加了 English 和日本語，不过有些不能自动变更语言的应用在更换系统语言为 English 之后变得很别扭。于是寻找可以单独设置应用语言的方法。</p>
<p>用惯了 Linux 再用 OS X 其实并没有那么容易的改变习惯… 当我准备尝试单独 export 一份 locale 再运行 app 的时候 OS X 直接告诉我不适用我的表情简直和伊莉雅一样。</p>
<p>…</p>
<p>睡了一觉起来继续 Google。找到了可以单独为应用设置语言并且 launch 的 app <a href="http://www.tj-hd.co.uk/en-gb/languageswitcher/" target="_blank" rel="external">Language Switcher</a>，看起来不错，但是每次启动 CLIP STUDIO PAINT 都要先打开这货，这不是我想要的效果。</p>
<p>于是最终找到一个合适的解决方案：用 <code>defaults</code> 命令。</p>
<p>原帖在<a href="http://hints.macworld.com/article.php?story=20061001065101830" target="_blank" rel="external">这里</a>。</p>
<p>设置 CLIP STUDIO PAINT Pro 语言环境命令：</p>
<figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">defaults <span class="keyword">write</span> jp.<span class="keyword">co</span>.celsys.CLIPSTUDIOPAINT.lip AppleLanguages <span class="string">'("en-US")'</span></div></pre></td></tr></table></figure>

<p>其中 <code>jp.co.celsys.CLIPSTUDIOPAINT.lip</code> 可以在应用的显示包信息 -&gt; Contents -&gt; Info.plist 中找到。</p>
<p>执行后就可以直接双击启动啦~</p>
]]></content>
    
    
      <category term="OS X" scheme="http://blog.phoenixlzx.com/tags/OS-X/"/>
    
      <category term="Notes" scheme="http://blog.phoenixlzx.com/categories/Notes/"/>
    
  </entry>
  
  <entry>
    <title><![CDATA[关于 SSL 和 IPKVM/VNC Applet 的笔记两则]]></title>
    <link href="http://blog.phoenixlzx.com/2014/10/14/2-notes-for-ssl-and-ipkvm/"/>
    <id>http://blog.phoenixlzx.com/2014/10/14/2-notes-for-ssl-and-ipkvm/</id>
    <published>2014-10-13T21:03:07.000Z</published>
    <updated>2014-10-15T03:28:57.000Z</updated>
    <content type="html"><![CDATA[<p>虽然是没啥关系的两个东西不过最近没啥可写的所以放在一块做个笔记。</p>
<h3 id="COMODO_SSL_CA_正确的串联姿势">COMODO SSL CA 正确的串联姿势</h3>
<p>有客户买了 COMODO Positive SSL 之后问我证书链变了要怎么串联。嘛确实文件变多了不过不至于这就不会了吧…</p>
<p>SSL 证书串联用 <code>cat</code> 命令的话顺序是自下而上，也就是 自己的证书 -&gt; 二级 Intermediate CA -&gt; 一级 Intermediate CA -&gt; Root CA</p>
<p>所以不管文件有多少这个顺序总是有的。以 COMODO Positive SSL 为例，假设域名为 <code>example.com</code>，<code>cat</code> 的串联命令：</p>
<figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line"><span class="input"><span class="prompt">cat example_com.crt COMODORSADomainValidationSecureServerCA.crt COMODORSAAddTrustCA.crt AddTrustExternalCARoot.crt &gt;</span> example_com.signed.crt</span></div></pre></td></tr></table></figure>

<p>于是就可以拿着 <code>example_com.signed.crt</code> 和生成 CSR 时一并生成的 <code>example_com.key</code> 去给 NGINX 咯。</p>
<p>附一份 NGINX 配置样例。</p>
<figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div><div class="line">25</div><div class="line">26</div><div class="line">27</div></pre></td><td class="code"><pre><div class="line"><span class="title">server</span> {</div><div class="line">      <span class="title">listen</span>      <span class="number">80</span>;</div><div class="line">      <span class="title">listen</span>      [::]:<span class="number">80</span> ipv6only=<span class="built_in">on</span>;</div><div class="line">      <span class="title">server_name</span> example.com;</div><div class="line">      <span class="comment">## redirect http to https ##</span></div><div class="line">      <span class="title">rewrite</span>       <span class="regexp"> ^</span> <span class="url">https://example.com$request_uri?</span> <span class="built_in">permanent</span>;</div><div class="line">}</div><div class="line"></div><div class="line"><span class="title">server</span> {</div><div class="line">    <span class="title">listen</span> <span class="number">443</span> ssl;</div><div class="line">    <span class="title">listen</span> [::]:<span class="number">443</span> ssl ipv6only=<span class="built_in">on</span>;</div><div class="line">    <span class="title">server_name</span> example.com;</div><div class="line"></div><div class="line">    <span class="title">ssl</span>                  <span class="built_in">on</span>; </div><div class="line">    <span class="title">ssl_certificate</span>      /etc/nginx/ssl/example_com.signed.crt;</div><div class="line">    <span class="title">ssl_certificate_key</span>  /etc/nginx/ssl/example_com.key;</div><div class="line">    <span class="title">ssl_protocols</span> SSLv3 TLSv1;</div><div class="line">    <span class="title">ssl_prefer_server_ciphers</span>   <span class="built_in">on</span>;</div><div class="line">    <span class="title">ssl_ciphers</span> ALL:!aNULL:!ADH:!eNULL:!LOW:!EXP:RC4+RSA:+HIGH:+MEDIUM;</div><div class="line">    <span class="title">keepalive_timeout</span>    <span class="number">70</span>;</div><div class="line">    <span class="title">ssl_session_cache</span>    shared:SSL:<span class="number">10m</span>;</div><div class="line">    <span class="title">ssl_session_timeout</span>  <span class="number">10m</span>;</div><div class="line">    </div><div class="line">    <span class="title">location</span> / {</div><div class="line">        <span class="comment"># ...</span></div><div class="line">    }</div><div class="line">}</div></pre></td></tr></table></figure>

<p>最后不忘打广告： [<a href="https://c.phoenixlzx.com" target="_blank" rel="external">https://c.phoenixlzx.com</a>]</p>
<h3 id="IPKVM/VNC_Applet_的使用">IPKVM/VNC Applet 的使用</h3>
<p>从 7u45 版本之后的 Java Webstart 就不能再直接执行 Ubersmith/SolusVM/Proxmox 等等的 IPKVM/VNC Applet 了。虽然实际上是这些商业软件的错不过还是很想让人骂 Java。</p>
<p>解决方案：下载一个旧版的 Java，比如 jdk6。</p>
<p>但是直接安装也是不能运行的，因为实际上调用的 java executable 是最新版 Java 的，所以一样报错。</p>
<p>如果是在 Linux 下，jre6 的目录在 <code>$HOME/jre6/jre1.6.0_38</code>，那么下载到 Applet 之后用下面的命令启动：</p>
<figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">applet_path=<span class="regexp">/path/</span>to<span class="regexp">/your/</span>applet.jnlp</div><div class="line">~<span class="regexp">/jre6/</span>jre1.<span class="number">6.0</span>_38<span class="regexp">/bin/</span>java -verbose -Xbootclasspath<span class="regexp">/a:~/</span>jre6<span class="regexp">/jre1.6.0_38/</span>lib<span class="regexp">/javaws.jar:~/</span>jre6<span class="regexp">/jre1.6.0_38/</span>lib<span class="regexp">/deploy.jar:~/</span>jre6<span class="regexp">/jre1.6.0_38/</span>lib<span class="regexp">/plugin.jar -classpath ~/</span>jre6<span class="regexp">/jre1.6.0_38/</span>lib<span class="regexp">/deploy.jar -Djava.security.policy=file:~/</span>jre6<span class="regexp">/jre1.6.0_38/</span>lib<span class="regexp">/security/</span>javaws.policy -DtrustProxy=<span class="keyword">true</span> -Xverify:remote -Djnlpx.home=~<span class="regexp">/jre6/</span>jre1.<span class="number">6.0</span>_38<span class="regexp">/bin -Dsun.awt.warmup=true -Djnlpx.origFilenameArg=$applet_path -Djnlpx.remove=false -esa -Xnoclassgc -Djnlpx.splashport=59999 -Djnlpx.jvm=~/</span>jre6<span class="regexp">/jre1.6.0_38/</span>bin<span class="regexp">/java -Djnlpx.vmargs="-esa -Xnoclassgc" com.sun.javaws.Main $applet_path</span></div></pre></td></tr></table></figure>

<h3 id="===_2014-10-15_更新_===">=== 2014-10-15 更新 ===</h3>
<p>SSLv3 <a href="http://googleonlinesecurity.blogspot.com/2014/10/this-poodle-bites-exploiting-ssl-30.html" target="_blank" rel="external">再次爆出漏洞</a>，在 Google 的新草案 <a href="https://tools.ietf.org/html/draft-ietf-tls-downgrade-scsv-00" target="_blank" rel="external">TLS_FALLBACK_SCSV</a> 还未明朗的情况下，目前禁用 SSLv3 是一个 <strong>不考虑 IE6</strong> 的 workaround。</p>
<figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line"><span class="function_or_atom">ssl_protocols</span> <span class="variable">TLSv1</span> <span class="variable">TLSv1</span>.<span class="number">1</span> <span class="variable">TLSv1</span>.<span class="number">2</span>;</div><div class="line"><span class="function_or_atom">ssl_ciphers</span> <span class="variable">ECDHE</span>-<span class="variable">RSA</span>-<span class="variable">AES256</span>-<span class="variable">SHA384</span>:<span class="variable">AES256</span>-<span class="variable">SHA256</span>:<span class="variable">RC4</span>:<span class="variable">HIGH</span>:<span class="exclamation_mark">!</span><span class="variable">MD5</span>:<span class="exclamation_mark">!</span><span class="function_or_atom">aNULL</span>:<span class="exclamation_mark">!</span><span class="function_or_atom">eNULL</span>:<span class="exclamation_mark">!</span><span class="variable">NULL</span>:<span class="exclamation_mark">!</span><span class="variable">DH</span>:<span class="exclamation_mark">!</span><span class="variable">EDH</span>:<span class="exclamation_mark">!</span><span class="variable">AESGCM</span>;</div><div class="line"><span class="function_or_atom">ssl_prefer_server_ciphers</span> <span class="function_or_atom">on</span>;</div><div class="line"><span class="function_or_atom">ssl_session_cache</span> <span class="function_or_atom">shared</span>:<span class="variable">SSL</span>:<span class="number">10</span>m;</div><div class="line"><span class="function_or_atom">ssl_session_timeout</span> <span class="number">10</span>m;</div></pre></td></tr></table></figure>

]]></content>
    
    
      <category term="SSL" scheme="http://blog.phoenixlzx.com/tags/SSL/"/>
    
      <category term="NGINX" scheme="http://blog.phoenixlzx.com/tags/NGINX/"/>
    
      <category term="Server" scheme="http://blog.phoenixlzx.com/tags/Server/"/>
    
      <category term="Configuration" scheme="http://blog.phoenixlzx.com/tags/Configuration/"/>
    
      <category term="IPKVM" scheme="http://blog.phoenixlzx.com/tags/IPKVM/"/>
    
      <category term="VNC" scheme="http://blog.phoenixlzx.com/tags/VNC/"/>
    
      <category term="Notes" scheme="http://blog.phoenixlzx.com/categories/Notes/"/>
    
  </entry>
  
  <entry>
    <title><![CDATA[2014·夏]]></title>
    <link href="http://blog.phoenixlzx.com/2014/09/02/2014-natsu/"/>
    <id>http://blog.phoenixlzx.com/2014/09/02/2014-natsu/</id>
    <published>2014-09-02T14:27:23.000Z</published>
    <updated>2014-09-02T14:45:06.000Z</updated>
    <content type="html"><![CDATA[<p>2014年的夏天应该是我一直以来度过的最繁忙和劳累的一段时光。多方面的压力、打击、困扰，加上自身各种情况条件不利，这个暑假真的让人感觉非常憋屈窝囊。</p>
<h3 id="实习">实习</h3>
<p>最值得一提的当然是我的第一份实习。大三下学期那会也就是想找个实习锻炼下自己，也可以给家里减轻一些经济负担。在 Twitter / V2EX 发布求职贴之后陆续有不少公司找上门来，大部分公司都给出了非常不错的条件，有些甚至在我明确答应之前就问我什么时候过去…虽然简历写得比较糟糕，不过仅仅通过和各种公司打交道还是见识到了一点互联网行业的竞争状态。<del>也算是头一回知道原来北京的创业公司工资可以给这么高</del></p>
<p>最终选择爱奇艺的原因大概就是 iven 前辈所说可以玩集群和虚拟化。虽然公司给的待遇实在让我不知道说啥比较好(除去房租几乎不够生活费，回学校一趟花掉一半工资在路费上…)，但是在经济状况还没有糟糕到不得不去待遇最高的公司的时候，我还是想多见识点东西学学经验。说实话爱奇艺没怎么让我失望，工位上就有质量非常好的千兆公网带宽，配备23寸 Dell 显示器(虽然主机配置不怎么样…)，弹性工作制，且几乎没有看到员工加班(虽然听说很多人回家后还是会挂上 VPN 连回公司继续工作)，办公环境有物业定时打扫，茶水间的阿姨遇到自动售货机有优惠活动就会通知大家，看我打了几个喷嚏还专门去帮我拿了药，公司对于新员工和实习生的成长也算是费了脑筋… </p>
<p>感受颇深的是我所在的团队。在校期间经常和已经参加工作的前辈们闲聊，前辈们吐槽和抱怨比较多的基本上就是一些比较奇葩的同事，其次是公司的种种不合理。我一直认为在公司遇到奇葩同事属于及其正常甚至一定会发生的现象，在决定动身去帝都的时候也做了很多心理准备和各种应对处理办法。不过来到之后发现我想太多… 团队成员友好和睦程度完全超出我的想像，完全没有遇到之前朋友们吐槽的种种情况。每天中午都是一起出去吃饭，路上一边挑餐厅挑半天一遍各种调侃…互相尊重、理解、关心、帮助几乎是整个团队互动的原则。</p>
<p>说到这里其实我有些愧疚。因为自身的很多原因，每天工作的效率都非常低，心情也相当不好，写代码自然也极其浮躁。iven 前辈每次 review 代码的时候都会提出一大堆完美主义的问题，要我一遍一遍修改重构，甚至有时候让我感觉「为什么要把一大堆坑死人不偿命的框架揉在一起，为什么我有一只处女座的 mentor」… 不过事实是我发现自己在写代码的时候越来越想着更优化的解决方案，框架内提供了解决方案的尽量不去用 hack 的方法。在面对一堆完全陌生的框架揉在一起的坑，且自己整个人都很不好的情况下，还是发挥出了平日 1/3 的开发效率。韧性应该有不少提高吧…</p>
<h3 id="跑路">跑路</h3>
<p>曾经津津乐道卖 VPS 的某某某又跑路了~ (为何是「又」…) 可是现在我也要干这种事。想想真是可笑。</p>
<p>其实跑路并不是一方原因直接决定，也算是很多方面的共同作用结果。一直以来 UltraKVM 项目都处于亏损状态且耗费了我大量的精力，但是由于整个虚拟化平台上还有很多朋友在跑的应用，我也还是一直坚持着做下去。8月份的攻击事件之后我把机房寄发的账单转给了吸引攻击流量的客户，却被喷小公司服务不靠谱。客户修改了自己的联系信息之后不见踪影(其实修改客户信息，财务面板是会发邮件的… 所有历史信息都会留底，所以不要觉得自己改了信息就找不到了)。无奈之中找朋友诉苦，意想不到地得到了帮助。于是向机房发邮件请求下架机器，并向他们一直以来的高质量服务表示感谢(MultaCOM 机房是我目前见过的最好的机房没有之一，不管是网络还是服务)。机房帮我取消了攻击流量的账单，在我指定的日期帮我下架机器并寄送到另外的机房。因此对 MC 机房也抱有很深的歉意。</p>
<p>为了这件事情，我三天没吃下饭，睡不着觉，通知客户备份数据的邮件发了一遍又一遍，担心用 sendmail 发会被丢到垃圾箱还用自己的个人邮箱发了两遍，承诺会在新机房上线并为所有有效客户提供为期至少一年的免费 VPS 服务。(吐槽：就这样还有人在下架之后找我说 VPS 为啥不能访问了云云让我感觉非常不爽，同时感谢朋友的帮助才让我有偿还客户的机会)。一直到给机房发送了最后要付的所有费用之后才算是舒坦了一些。</p>
<h3 id="权限狗">权限狗</h3>
<p>暑假里另外一件闹得比较欢乐的事情是喵窝服务器。应该是敖厂长的原因(?) 整个姬家都可以看到很多人在玩 Minecraft。一边在姬家宣传，管理组也是费尽了心思来准备向墙内玩家开放申请。</p>
<p>其实向墙内玩家开放也是我主要在推，因为墙内确实很多高素质的玩家(先不说游戏技术如何)，我很希望能吸引到这些玩家来玩。17精心策划了宣传帖，二小姐把之前用的服务器寄到了帝都，我和壕桥手忙脚乱地把服务器送到机房调试了一个晚上，我还花了两个小时写了申请表单的程序，都是在为了迎接新玩家所做的准备。</p>
<p>不过事实上情况并没有我们想像得那么好。前三天来申请的玩家里，过审率只有不到1/12，接下来的两天里更是掉到了 1/20。从开放申请到开学之前，几乎每天都是好几十封白名单申请邮件。前几天去大概看了下，过审率依旧是 1/15 的样子。不过就算如此严格的审核，我们依然遇到了漏网之鱼…</p>
<p>事件经过详细说的话估计今晚我就不用睡觉了。大致就是先后有三个熊孩子在服里发飙被封，一个是要在毫无干系的百度贴吧到处发帖把我和其他几个管理骂了一通，要和我「正面刚」(…错字？)，另一个则恐吓我说要去 mcbbs 「好好说说这件事」。再后面则是一个熊孩子根本没过审核，在群里刷屏伸手被拒绝后骂我们对新手不友好，是「虐待他」…..于是目前还记得的就这仨。</p>
<p>熊孩子被封么无非就是到处发帖子骂，是不是相关论坛也不管(素质…)，然后拼命要求加群加好友然后在附言里破口大骂权限狗一类的词，或者是各种无理取闹要求入群入服或者挑衅之类。这么看咱这开源社区的管理员都算是非常好当的了233，对付熊孩子咱也只有放置 play 咯。”Argue with idiots, and you become an idiot.” — <em>Hackers and Painters</em></p>
<h3 id="客户中心">客户中心</h3>
<p>从大一开始我就在做各种域名/主机/数字证书的代理商，来找我买的都是姬家或者其他 SNS 的朋友，我也没怎么去宣传，权当是自己用也方便下朋友用吧。不过麻烦在于一个域名后缀在几家注册商都可以注册这样的问题，客户为了拿到更低的价格光在我这里就要注册好几个帐户。因此整合自己的业务，做统一的客户中心算是早就有的想法。在返校前两天的时候终于抽时间搭了起来，顺便用掉了两张阿里云的抵用券。广告：地址在 [<a href="https://c.phoenixlzx.com" target="_blank" rel="external">https://c.phoenixlzx.com</a>]</p>
<p>于是麻烦又来了——</p>
<p>坑：换了域名，支付宝的接口要重新审核。一连三次审核失败：</p>
<ul>
<li>第一次：网站无法访问——泥煤啊我是 https 好吗！</li>
<li>第二次：网站非中文——泥煤啊我有多语言支持的好吗！另外狗爹那样的专业坑爹英文站怎么过审的？！</li>
<li>第三次：网站未备案——泥煤啊我之前一样不备案怎么过审的？！一个二级域名你给我备案试试看啊！</li>
</ul>
<p>于是干脆不提交审核了，我想其他办法用。</p>
<p>坑：Hostbill 默认货币选择的 USD，于是支付宝接口愣是不显示。不要那么智能好吗？敢不敢在结帐的时候自动转换？！</p>
<p>然后我批量更新了下产品价格，然后手残误操作把价格提太高了，想再降回去，于是玩完：所有产品的价格配置坏掉了。200多个产品啊！我一个个改得心脏都早搏了………</p>
<p>坑：Hostbill 自己的 whois 文件有问题，很多域名后缀无法正确查询可用性。Google 了半天找到一个论坛的帖子有解决方案，却没有提供 patch 过的文件。没办法自己一点点按照修改步骤打了 patch。由于在公司被 mentor 带出来的习惯，每一处独立的变更都会 commit 一次，然后创了一晚上80个 commit 的记录…..壕桥我超过你的一天50个记录了！</p>
<p>剩下没有的域名自己按照类似的方法也修了，文件丢在 <a href="https://github.com/phoenixlzx/hostbill-whois" target="_blank" rel="external">Github</a>。</p>
<h3 id="返校">返校</h3>
<p>学校的事情一直想等毕业以后再写，写本文的时候也纠结了很久，最后还是稍微写一点点。本来公司的项目第一个版本就要上线，还差最后的几个功能，突然接到母上大人的电话要求我返校报到。直接结果是——我下个月生活费不够了。</p>
<p>然后在学校便是各种跑宿舍分配、联系任课老师等等等…</p>
<p>比较让我提起精神的事情是发现社团办公室整洁了不少，书架上也放满了各种著名的计算机类书籍。新任主席也不像以前那么傲娇，而且一个月写出了一套很不错的前端页面(blueed 老伙计你可以安心的去了…</p>
<h3 id="归京">归京</h3>
<p>明天就回帝都了。今晚好好休息… 虽然还有很多憋屈的事情根本都没提，不过也算是吐出来不少。希望回去公司可以有更好的状态吧。</p>
<p>晚安，好梦。</p>
]]></content>
    
    
      <category term="Life" scheme="http://blog.phoenixlzx.com/categories/Life/"/>
    
  </entry>
  
  <entry>
    <title><![CDATA[在 Ubuntu 服务器上搭建 OpenConnect 服务器小记]]></title>
    <link href="http://blog.phoenixlzx.com/2014/07/21/setup-openconnect-server-on-ubuntu/"/>
    <id>http://blog.phoenixlzx.com/2014/07/21/setup-openconnect-server-on-ubuntu/</id>
    <published>2014-07-21T05:21:57.000Z</published>
    <updated>2014-07-21T09:11:30.000Z</updated>
    <content type="html"><![CDATA[<p>最近猫给推荐的 Cisco AnyConnect，面基的时候也看到 Aveline 菊苣在用 AnyConnect(插嘴：正太菊苣果然好可爱！！！) 于是自己折腾了下。作为少有的能让我折腾起来的 VPN，暂且把搭建步骤简单记录下。</p>
<p>AnyConnect 的好处是基于 HTTPS，证书可以申请 StartSSL 的，而且配置也不很复杂。另外配置文件里发现了很多专门为商业化/企业服务定制的选项，例如最大同时在线客户端数量，同账户最大在线设备数量等等。</p>
<p>OpenConnect 是 AnyConnect 的开源实现。目前 0.8.0 版本需要 GnuTLS 3.1 以上版本，所以我们就直接在 Ubuntu 14.04 中搭建。另外 就算是基于 HTTPS，这货也是要用 TUN 设备的，所以 OpenVZ 用户们请注意。</p>
<h3 id="准备">准备</h3>
<p>命令</p>
<figure class="highlight bash"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">apt-get install build-essential libwrap0-dev libpam0g-dev libdbus-<span class="number">1</span>-dev \</div><div class="line">  libreadline-dev libnl-route-<span class="number">3</span>-dev libprotobuf-c0-dev libpcl1-dev libopts25-dev \</div><div class="line">  autogen libgnutls28 libgnutls28-dev libseccomp-dev libhttp-parser-dev</div></pre></td></tr></table></figure>

<h3 id="安装">安装</h3>
<p>下载源码</p>
<figure class="highlight bash"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">wget -c ftp://ftp.infradead.org/pub/ocserv/ocserv-<span class="number">0.8</span>.<span class="number">0</span>.tar.xz</div><div class="line">tar xvf ocserv-<span class="number">0.8</span>.<span class="number">0</span>.tar.xz</div><div class="line"><span class="built_in">cd</span> ocserv-<span class="number">0.8</span>.<span class="number">0</span></div></pre></td></tr></table></figure>

<p>编译安装</p>
<figure class="highlight bash"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line">./configure --prefix=/opt/ocserv </div><div class="line">make</div><div class="line">make install</div><div class="line">mkdir /opt/ocserv/etc/</div><div class="line">cp doc/sample.config /opt/ocserv/etc/config</div></pre></td></tr></table></figure>

<h3 id="配置">配置</h3>
<p>这里只记录需要修改的重点配置，其他配置请参照样例配置文件的注释按需修改。</p>
<p>文件 <code>/opt/ocserv/etc/config</code></p>
<figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div></pre></td><td class="code"><pre><div class="line"><span class="variable">auth =</span> <span class="string">"plain[/opt/ocserv/etc/passwd]"</span></div><div class="line"><span class="variable">listen-host =</span> connect.your.domain</div><div class="line"><span class="variable">max-clients =</span> <span class="number">128</span></div><div class="line"><span class="variable">max-same-clients =</span> <span class="number">4</span></div><div class="line"><span class="variable">server-cert =</span> /opt/ocserv/etc/ssl/server-cert.pem</div><div class="line"><span class="variable">server-key =</span> /opt/ocserv/etc/ssl/server-key.pem</div><div class="line"><span class="variable">ca-cert =</span> /opt/ocserv/etc/ssl/ca.pem</div><div class="line"><span class="variable">mobile-idle-timeout =</span> <span class="number">2400</span></div><div class="line"><span class="variable">ipv4-network =</span> <span class="number">192.168</span>.<span class="number">1.0</span></div><div class="line"><span class="variable">ipv4-netmask =</span> <span class="number">255.255</span>.<span class="number">255.0</span></div><div class="line"><span class="variable">dns =</span> <span class="number">8.8</span>.<span class="number">8.8</span></div><div class="line"><span class="variable">dns =</span> <span class="number">8.8</span>.<span class="number">4.4</span></div><div class="line"><span class="variable">route =</span> <span class="number">192.168</span>.<span class="number">1.0</span>/<span class="number">255.255</span>.<span class="number">255.0</span></div><div class="line"><span class="variable">route-add-cmd =</span> <span class="string">"ip route add 192.168.1.0 dev tun0"</span></div><div class="line"><span class="variable">route-del-cmd =</span> <span class="string">"ip route delete 192.168.1.0 dev tun0"</span></div></pre></td></tr></table></figure>

<p>创建用户，命令</p>
<figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line"><span class="regexp">/opt/</span>ocserv<span class="regexp">/bin/</span>ocpasswd -c <span class="regexp">/opt/</span>ocserv<span class="regexp">/etc/</span>passwd username</div></pre></td></tr></table></figure>

<p>按提示输入两次密码。</p>
<p>iptables 规则</p>
<figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line"><span class="input"><span class="prompt">iptables -t nat -A POSTROUTING -j SNAT --to-source &lt;server ip&gt;</span> -o &lt;nic&gt;</span></div></pre></td></tr></table></figure>

<p>记得把 <code>&lt;server ip&gt;</code> 和 <code>&lt;nic&gt;</code> 改为服务器公网 IP 和对应网卡的名称。</p>
<h3 id="搞定">搞定</h3>
<p>折腾完毕，AnyConnect 客户端可以成功使用了。</p>
<p><del>不过呢折腾完之后发现这货其实被干扰得很厉害啊…</del></p>
]]></content>
    
    
      <category term="Server" scheme="http://blog.phoenixlzx.com/tags/Server/"/>
    
      <category term="Ubuntu" scheme="http://blog.phoenixlzx.com/tags/Ubuntu/"/>
    
      <category term="Linux" scheme="http://blog.phoenixlzx.com/tags/Linux/"/>
    
      <category term="Network" scheme="http://blog.phoenixlzx.com/tags/Network/"/>
    
      <category term="VPN" scheme="http://blog.phoenixlzx.com/tags/VPN/"/>
    
      <category term="iptables" scheme="http://blog.phoenixlzx.com/tags/iptables/"/>
    
      <category term="Notes" scheme="http://blog.phoenixlzx.com/categories/Notes/"/>
    
  </entry>
  
  <entry>
    <title><![CDATA[使用 iptables 过滤 DNS 放大攻击]]></title>
    <link href="http://blog.phoenixlzx.com/2014/05/23/iptables-rate-limiting-rules-to-block-dns-amplification-attack/"/>
    <id>http://blog.phoenixlzx.com/2014/05/23/iptables-rate-limiting-rules-to-block-dns-amplification-attack/</id>
    <published>2014-05-23T12:52:59.000Z</published>
    <updated>2014-05-23T22:17:50.000Z</updated>
    <content type="html"><![CDATA[<p>昨晚开始我们的 DNS 日志中出现大量的针对<code>wradish.com</code>的<code>ANY</code>记录请求，导致大量正常解析请求超时。今天上午到中午时分再次出现流量更大、针对<code>ping.zong.co.ua</code>的<code>ANY</code>记录请求，导致整个 DNS 服务瘫痪，完全无法响应正常解析请求。</p>
<p>检查后发现 <code>wradish.com</code> 早已因多次 DNS 放大攻击而被列入开放 DNS 服务器黑名单，网络上也有大量的针对该域名攻击请求包的过滤方法。<code>ping.zong.co.ua</code> 却完全没有任何搜索记录。</p>
<p>DNS 放大攻击 (DNS Amplification Attack) 即通过向多台 DNS 开放服务器发送无意义的 recursive 解析请求，从而使得根域名服务器遭到超大流量的解析请求包，无暇顾及正常的解析请求，由此实现分布式拒绝服务攻击 (Distributed Denial of Service)。我们的 DNS 服务器在这里既是攻击者，也是受害者。</p>
<p>DNSMasq 默认允许 recursive 请求，且似乎本身就没有设计为开放 DNS 服务，所以貌似也没有办法禁用掉 recursive 请求。因此我们使用 <a href="http://blog.dataforce.org.uk/2013/08/limiting-the-effectiveness-of-dns-amplification/" target="_blank" rel="external">iptables 规则</a>来过滤掉这些请求。</p>
<figure class="highlight bash"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div></pre></td><td class="code"><pre><div class="line"><span class="comment"># Create a chain to store block rules in</span></div><div class="line">iptables -N BADDNS </div><div class="line"></div><div class="line"><span class="comment"># Match all "IN ANY" DNS Queries, and run them past the BADDNS chain.</span></div><div class="line">iptables -A INPUT -p udp --dport <span class="number">53</span> -m string --hex-string <span class="string">"|00 00 ff 00 01|"</span> --to <span class="number">255</span> --algo bm -m comment --comment <span class="string">"IN ANY?"</span> -j BADDNS</div><div class="line">iptables -A FORWARD -p udp --dport <span class="number">53</span> -m string --hex-string <span class="string">"|00 00 ff 00 01|"</span> --to <span class="number">255</span> --algo bm -m comment --comment <span class="string">"IN ANY?"</span> -j BADDNS</div><div class="line"></div><div class="line"><span class="comment"># Block domains that are being used for DNS Amplification...</span></div><div class="line">iptables -A BADDNS -m string --hex-string <span class="string">"|04 72 69 70 65 03 6e 65 74 00|"</span> --algo bm -j DROP --to <span class="number">255</span> -m comment --comment <span class="string">"ripe.net"</span></div><div class="line">iptables -A BADDNS -m string --hex-string <span class="string">"|03 69 73 63 03 6f 72 67 00|"</span> --algo bm -j DROP --to <span class="number">255</span> -m comment --comment <span class="string">"isc.org"</span></div><div class="line">iptables -A BADDNS -m string --hex-string <span class="string">"|04 73 65 6d 61 02 63 7a 00|"</span> --algo bm -j DROP --to <span class="number">255</span> -m comment --comment <span class="string">"sema.cz"</span></div><div class="line">iptables -A BADDNS -m string --hex-string <span class="string">"|09 68 69 7a 62 75 6c 6c 61 68 02 6d 65 00|"</span> --algo bm -j DROP --to <span class="number">255</span> -m comment --comment <span class="string">"hizbullah.me"</span></div><div class="line"></div><div class="line"><span class="comment"># Rate limit the rest.</span></div><div class="line">iptables -A BADDNS -m recent --set --name DNSQF --rsource</div><div class="line">iptables -A BADDNS -m recent --update --seconds <span class="number">10</span> --hitcount <span class="number">1</span> --name DNSQF --rsource -j DROP</div></pre></td></tr></table></figure>

<p>添加以上规则后执行 <code>iptables-save</code> 来使更改生效。</p>
<p>目前为止似乎没有报告出现了明显规模的 DNS 放大攻击征兆，攻击者针对我们防火墙封锁的反应速度、攻击所用的记录以及从日志中发现的大量不同请求源也暗示着我们的服务器可能是最终攻击目标，用攻击流量消耗尽服务器带宽而导致服务瘫痪。至于原因，我也没有想明白为何会有攻击者对一个小型私用服务器感兴趣。</p>
<p>PS. 到本文发表时，攻击依旧在继续，且流量越来越大。目前阿里云的云盾设置可以说无力，只能依靠 iptables 暂时缓解。后续的应对计划仍在制定中。</p>
<p>=== 2014-05-24 更新 ===</p>
<p>Aveline 菊苣给出了一个<a href="https://github.com/smurfmonitor/dns-iptables-rules" target="_blank" rel="external">DNS Amplification IPTABLES block lists</a> 给点128个赞！</p>
]]></content>
    
    
      <category term="iptables" scheme="http://blog.phoenixlzx.com/tags/iptables/"/>
    
      <category term="DNS" scheme="http://blog.phoenixlzx.com/tags/DNS/"/>
    
      <category term="Network" scheme="http://blog.phoenixlzx.com/tags/Network/"/>
    
      <category term="Network" scheme="http://blog.phoenixlzx.com/categories/Network/"/>
    
  </entry>
  
  <entry>
    <title><![CDATA[在异步方法中使用 for 循环，以及循环中包含异步方法的几点坑]]></title>
    <link href="http://blog.phoenixlzx.com/2014/05/19/for-loop-in-async-function-with-async-in-it/"/>
    <id>http://blog.phoenixlzx.com/2014/05/19/for-loop-in-async-function-with-async-in-it/</id>
    <published>2014-05-18T19:18:20.000Z</published>
    <updated>2014-05-19T09:07:12.000Z</updated>
    <content type="html"><![CDATA[<p>又是熬夜干活。</p>
<p>之前在 StackOverflow 上看到过 <a href="http://stackoverflow.com/questions/5050265/javascript-nodejs-is-array-foreach-asynchronous" target="_blank" rel="external">Array.forEach 是同步(阻塞)的</a>，但是实际使用的时候，特别是我在写 Node.js 程序的时候几乎没感觉到过它的阻塞—— forEach 或者其他 for 循环之后的 callback 从来都是被立即调用，没有等待整个循环执行完。&lt;== 这个命题是个假命题，下面会说明。</p>
<p>今天又一次掉进异步的坑里才发现 for 循环确实是阻塞的。至于为何它后面的回调函数会立即被调用，原因是 for 循环里有异步方法。整个循环不会等待异步方法结束，而是直接进入下一个循环，所以它 <strong>看起来</strong> 像是异步的，后面的回调函数「立即」被调用了。<a href="http://stackoverflow.com/questions/21184340/async-for-loop-in-node-js" target="_blank" rel="external">StackOverflow</a> 上有更详细的例子和说明。</p>
<p>不过如果是数组，那么直接使用 <a href="https://github.com/caolan/async" target="_blank" rel="external">async</a> 替代原生的 forEach 和其他 for 语句即可简单解决。但是问题出在我这里的情况是要用 <code>for in</code> 语句处理 Object，虽说 Javascript 里 Object 和 Array 非常相似但是毕竟没有 Array 那些好用的方法，在这种情况下也不方便使用 prototype 来给我的 object 新增类似方法。二小姐给出的解决方案是手动设置计数器，获得 object 长度的办法是使用 <code>Object.keys(obj).length</code>。</p>
<p><strong>注意</strong> <code>Object.keys(obj).length</code> 并不一定是要循环的次数，具体需要看 Object 的结构。我这里就是解析 DOM 得到的 Object 所以有额外的东西，<code>length - 3</code> 才是需要的结果。</p>
<p>代码写出来大概是这个样子</p>
<figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div><div class="line">25</div><div class="line">26</div><div class="line">27</div><div class="line">28</div><div class="line">29</div><div class="line">30</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span><span class="params">(obj, callback)</span> </span>{</div><div class="line"></div><div class="line">    <span class="keyword">var</span> tasks = <span class="built_in">Object</span>.keys(obj).length;</div><div class="line">    </div><div class="line">    <span class="keyword">var</span> arr = [<span class="string">'one'</span>, <span class="string">'two'</span>, <span class="string">'three'</span>];</div><div class="line">    </div><div class="line">    <span class="keyword">for</span> (key <span class="keyword">in</span> obj) {</div><div class="line">	</div><div class="line">	tasks--;</div><div class="line">	</div><div class="line">	async.eachSeries(arr, someFunc(n, cb) {</div><div class="line">	</div><div class="line">	    <span class="comment">// do stuff here.</span></div><div class="line">	    </div><div class="line">	    cb();</div><div class="line">	</div><div class="line">	}, <span class="function"><span class="keyword">function</span><span class="params">(err)</span> </span>{</div><div class="line">	</div><div class="line">	    <span class="comment">// operations after array iteration completed.</span></div><div class="line">	    </div><div class="line">	    <span class="keyword">if</span> (tasks === <span class="number">0</span>) {</div><div class="line">	    </div><div class="line">		<span class="comment">// All tasks done, return to callback</span></div><div class="line">		<span class="keyword">return</span> callback();</div><div class="line">	    </div><div class="line">	    }</div><div class="line">	    </div><div class="line">	});</div><div class="line">    }</div><div class="line">}</div></pre></td></tr></table></figure>

<p>判断的语句其实也可以放在 <code>async.eachSeries</code> 之后，这样可以明显减少判断的计算(虽然那点计算不算啥) 但是也有可能会在没等待最后一次 async 执行结束的时候就返回了，所以保险起见还是多花点处理器来保证全部任务执行完。以及在最后 <code>callback</code> 的时候如果不写 <code>return</code> 就会失效。我还没查原因，因为已经累得不行了… 所以(当回伸手党)如果哪位童鞋知道的麻烦告知… 有灵梦的节操相送哦 &gt;///&lt;</p>
<p>==== 2014-05-20 更新 ====</p>
<p>感谢 Xuetian Weng 菊苣为了灵梦的节操不远万里给我纠错 /w\ 上面的代码在 <code>async.eachSeries</code> 里有异步方法时存在多次 <code>tasks === 0</code> 的情况，因为同样的因为 <code>for in</code> 循环不会等待 async 结束，async 的回调不一定会在 <code>tasks</code> 下一次自减之前被调用。解决办法是 <code>tasks--</code> 放在 <code>async.eachSeries</code> 的回调函数里。更好些的代码如下：</p>
<figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div><div class="line">25</div><div class="line">26</div><div class="line">27</div><div class="line">28</div><div class="line">29</div><div class="line">30</div><div class="line">31</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span><span class="params">(obj, callback)</span> </span>{</div><div class="line"></div><div class="line">    <span class="keyword">var</span> tasks = <span class="built_in">Object</span>.keys(obj).length;</div><div class="line"></div><div class="line">    <span class="keyword">var</span> arr = [<span class="string">'one'</span>, <span class="string">'two'</span>, <span class="string">'three'</span>];</div><div class="line"></div><div class="line">    <span class="keyword">for</span> (key <span class="keyword">in</span> obj) {</div><div class="line"></div><div class="line">        async.eachSeries(arr, someFunc(n, cb) {</div><div class="line"></div><div class="line">            <span class="comment">// do stuff here.</span></div><div class="line"></div><div class="line">            cb();</div><div class="line"></div><div class="line">        }, <span class="function"><span class="keyword">function</span><span class="params">(err)</span> </span>{</div><div class="line"></div><div class="line">            <span class="comment">// operations after array iteration completed.</span></div><div class="line">	    </div><div class="line">            <span class="comment">// tasks self-decrease should be here to ensure every async.eachSeries call has ended.</span></div><div class="line">            tasks--;</div><div class="line"></div><div class="line">            <span class="keyword">if</span> (tasks === <span class="number">0</span>) {</div><div class="line"></div><div class="line">                <span class="comment">// All tasks done, return to callback</span></div><div class="line">                <span class="keyword">return</span> callback();</div><div class="line"></div><div class="line">            }</div><div class="line"></div><div class="line">        });</div><div class="line">    }</div><div class="line">}</div></pre></td></tr></table></figure>

<p>不过菊苣说最后的 return 是可有可无，我这里的测试是不加 return (不管是在前面还是后面) 这个 callback 都不会被正确调用… 所以 return 的问题并没有算是解决，于是放到后面有时间的话来满满折腾吧~看 Xuetian Weng 菊苣如此渴求节操的表情咱就给一点表示感谢好了呜(双手奉上</p>
]]></content>
    
    
      <category term="Node.js" scheme="http://blog.phoenixlzx.com/tags/Node-js/"/>
    
      <category term="Node.js" scheme="http://blog.phoenixlzx.com/categories/Node-js/"/>
    
  </entry>
  
  <entry>
    <title><![CDATA[修复 Arch Rollback Machine 磁盘占用]]></title>
    <link href="http://blog.phoenixlzx.com/2014/05/17/repo-arm-disk-rescue-and-resize/"/>
    <id>http://blog.phoenixlzx.com/2014/05/17/repo-arm-disk-rescue-and-resize/</id>
    <published>2014-05-17T07:59:48.000Z</published>
    <updated>2014-05-17T00:43:49.000Z</updated>
    <content type="html"><![CDATA[<p>最近发现 downgrade 失效了，降级啥包都是直接错误退出。然后排查的时候发现是 Arch Rollback Machine 挂了，原因是——磁盘不足。</p>
<p>检查后发现 pm2 的日志有 7G，850天前的包有5000多个，365天内的包有10万余，磁盘已经被吃得满满当当。</p>
<p>日志好说，因为没啥特别有用的东西也不用拿来做分析，直接删掉就好。Felix 也把 850 天前的包全部删干净了。不过再次检查的时候 <code>df -h</code> 显示依旧是磁盘被吃满的，而 <code>df -ih</code> 显示 inode 占用没有问题。猫怀疑是有文件句柄还开着，但是把所有可能的进程杀掉之后依旧没有变化，排除。另外一个现象是文件删除进程完成后过了一段时间磁盘开始有少量空闲空间了，可能是磁盘缓存…? </p>
<p>于是靠着这空出来的 900M 多点的空间执行了几个操作：把有 HTTP flood 漏洞的 Node.js 0.10.16 升级到最新版 0.10.28，将包同步的 cronjob 降低到每天执行一次，压缩旧的 NGINX 日志。</p>
<p>以及用户一直要求的包归档功能，实际上是已经有了的，但是并没通过 NGINX 配置来实现(Pia!&lt;(=ｏ ‵-′)ノ☆猫) 所以准备把归档功能写到 API 服务端里。因为之前很有预见性的同步的时候把每天的包数据库按照 <code>/year/month/day/repo</code> 的结构保存起来(然后这部分的数据库文件就有 30G！)，所以用户按照这个路径来请求包数据库的时候直接把对应的数据库文件丢给客户端，请求包文件的时候则直接从包目录里丢过去。这样的话其实从一个较早的时间结构最后请求一个较新的包文件也能拿到….不过这个不是问题，因为包归档给 pacman 用的话，有哪些包还是 pacman 要读数据库的。</p>
<p>包归档的 index 确实花了不少时间，目前的实现貌似也不很好，虽然貌似还挺快的，但是也挺吃硬盘的…而且一连好几次手残敲错了东西、少加了分隔符、加了冗余的配置变量…. 总之就是爆肝熬夜的效率超级差，4点多基本上没问题的时候已经能感觉到身体很不舒服了…</p>
<p>然后是一觉睡到第二天中午。觅食归来给 A.R.M 的 KVM 虚拟机硬盘增加了 100G。执行 <code>resize2fs</code> 的时候报错说已经达到最大空间，删掉 swap 之后依旧。接下来干了一件很作死的事情——把所有分区删掉了，然后按照原来的分区方案重建了新的分区。Reboot 之后 lsblk 是对了，但是 df -h 依旧是原来的分区。一叶说是 resize 没成功，要进 LiveCD 里面 resize2fs。不过我没镜像… 按照一叶的建议挂上 VNC 进 recovery 然后 fsck，再正常进系统执行 <code>resize2fs</code>，成功。</p>
<p>回过神来的时候发现这种作死的行为我居然手都没抖一下 = = 几百G的数据呐…</p>
<p>晚上还要做字幕，再去休息下好了…</p>
]]></content>
    
    
      <category term="Server" scheme="http://blog.phoenixlzx.com/tags/Server/"/>
    
      <category term="Ubuntu" scheme="http://blog.phoenixlzx.com/tags/Ubuntu/"/>
    
      <category term="Linux" scheme="http://blog.phoenixlzx.com/tags/Linux/"/>
    
      <category term="Filesystem" scheme="http://blog.phoenixlzx.com/tags/Filesystem/"/>
    
      <category term="Linux" scheme="http://blog.phoenixlzx.com/categories/Linux/"/>
    
  </entry>
  
  <entry>
    <title><![CDATA[为国内用户加速 Apple 服务]]></title>
    <link href="http://blog.phoenixlzx.com/2014/05/15/accelerate-apple-services-in-cn/"/>
    <id>http://blog.phoenixlzx.com/2014/05/15/accelerate-apple-services-in-cn/</id>
    <published>2014-05-15T13:57:19.000Z</published>
    <updated>2014-05-15T06:24:46.000Z</updated>
    <content type="html"><![CDATA[<p>偶然看到<a href="https://plus.google.com/105938465531761409080/posts/3EXLAUerxMD" target="_blank" rel="external">雅诗的po</a> 才发觉自己 DNS 的 Apple 服务加速已经几乎不能用了，速度非常慢。倒是挺奇怪的，因为自己做的 DNS 服务器对于整个 Apple 域的上游都是 V2EX DNS，难道是 V2EX DNS 出问题了？</p>
<p>总之先不管那些。现在来简单做一份 Apple 服务加速的配置。</p>
<p>稍微 Google 下 Apple 的服务地址，目前获得的地址如下：</p>
<figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">a<span class="list">{1-2000}</span>.phobos.apple.com</div><div class="line">swdist.apple.com</div><div class="line">itunes.apple.com</div><div class="line">contentdelivery.itunes.apple.com</div></pre></td></tr></table></figure>

<p>对应在国内的 CDN 地址是</p>
<figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">a1-a200.phobos.apple.chinacache.net</div><div class="line">swdist.apple.ccgslb.net</div><div class="line">itunes.apple.ccgslb.<span class="keyword">com</span>.<span class="keyword">cn</span></div><div class="line">(最后一个没找到CDN地址..)</div></pre></td></tr></table></figure>

<p>然后根据 114DNS 和 Google DNS 综合查询以上地址的 IP。顺便发现地址里有 <code>cnc</code>, <code>cncssr</code> 这样的关键字，于是正好尝试把 <code>cnc</code> 改成 <code>tel</code> 于是得到了电信节点的 IP。</p>
<p>现在写一个两句话的 js 脚本来生成一份针对 <code>a{1-2000}.phobos.apple.com</code> 的加速列表(废话这种东西能手写吗要累死啦)，以达到尽可能将电信、联通的节点混在一起，保证各个 ISP 用户的下载速度。</p>
<figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div></pre></td><td class="code"><pre><div class="line"><span class="comment">// Accelerate IPs for Apple download service.</span></div><div class="line"><span class="comment">// apple-ips.js</span></div><div class="line"><span class="keyword">var</span> ips = [</div><div class="line">  <span class="string">'221.192.144.12'</span>,</div><div class="line">  <span class="string">'182.118.46.137'</span>,</div><div class="line">  <span class="string">'124.95.150.148'</span>,</div><div class="line">  <span class="string">'123.235.32.2'</span>,</div><div class="line">  <span class="string">'119.84.69.17'</span>,</div><div class="line">  <span class="string">'122.228.85.196'</span>,</div><div class="line">  <span class="string">'115.231.150.15'</span>,</div><div class="line">  <span class="string">'61.164.153.7'</span>,</div><div class="line">  <span class="string">'183.57.28.18'</span></div><div class="line">]</div><div class="line"></div><div class="line"><span class="keyword">for</span> (<span class="keyword">var</span> i = <span class="number">1</span>; i &lt;= <span class="number">2000</span>; i++) {</div><div class="line">  <span class="built_in">console</span>.log(ips[<span class="built_in">Math</span>.floor(<span class="built_in">Math</span>.random()*ips.length)] + <span class="string">' a'</span> + i + <span class="string">'.phobos.apple.com'</span>);</div><div class="line">}</div></pre></td></tr></table></figure>

<p>然后</p>
<figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line"><span class="input"><span class="prompt">~&gt;</span> node apple-ips.js &gt; apple.conf</span></div></pre></td></tr></table></figure>

<p>就得到一份可以直接用的加速列表啦。</p>
]]></content>
    
    
      <category term="Configuration" scheme="http://blog.phoenixlzx.com/tags/Configuration/"/>
    
      <category term="Notes" scheme="http://blog.phoenixlzx.com/categories/Notes/"/>
    
  </entry>
  
  <entry>
    <title><![CDATA[新的 NJLUG 主站]]></title>
    <link href="http://blog.phoenixlzx.com/2014/05/07/new-njlug-homepage/"/>
    <id>http://blog.phoenixlzx.com/2014/05/07/new-njlug-homepage/</id>
    <published>2014-05-07T03:46:09.000Z</published>
    <updated>2014-05-06T20:21:46.000Z</updated>
    <content type="html"><![CDATA[<p>南京 Linux 用户组有一段时间没有聚会了。偶然想起来之前我用 Jekyll 折腾的那个主页，随手敲了主页地址… 啊啦，还基本上没变啊…</p>
<p>嘛虽然自己写了那么几个东西，不过在组里待了也有一段时间也学到了不少东西。用户对社区的回报应该就在这时候展现出来吧。</p>
<p>依旧是 Express.js 框架，依旧是 Bootstrap。不过这回不想用数据库了，改用文本存储。除了主页和关于，顺便集成一个 Planet 在里面。RSS feed 的话，除了遍历一遍 upsert (不存在的添加，已存在的跳过)之外没想到什么更好的办法。实际上本来就是 RSS 没必要存储所有的文章，更何况又不是做之前想做的类似 Google Reader 的东西。</p>
<p>目前的 Planet 工作方式为首先检测是否存在文章数据文件 <code>posts.array</code>，如果不存在则读取 <code>planet.list</code> 中的订阅链接，获取到文章后 push 到一个数组中，按照发表日期排序并保存在 <code>posts.array</code> 里。到底是先排序再保存还是先存进去，渲染页面的时候再排序，这个稍微纠结了下。因为自己的配置文件里更新频率是 1 hour, 而且似乎每次更新到的文章数量还挺多的。所以保存时排序的话，则每天固定排序23次；如果渲染时排序的话，就看每天能有几个访问了…. 然后发现自己在这种小问题上纠结个毛毛啊啊啊啊啊。</p>
<p>排序后保存进文件之前和从文件读出之后要有两个 JSON 操作。保存之前 <code>JSON.stringify(posts)</code> 转换成字符串再保存，否则文件里是 <code>[[object Object], [object Object]]</code>。读出来之后 <code>JSON.parse(posts)</code> 再转换成对象数组，否则直接就一字符串没办法处理。</p>
<p>接下来就是主页和关于。因为不想写用户系统了所以就不发文章了吧… 关于可以是万年不变的页面，首页的话，显示最近的公告好了。最简单的实现就是存两个 markdown 文件然后用 marked 渲染出来插在页面里。</p>
<p>全部的源码在<a href="https://github.com/phoenixlzx/NJLUG-site" target="_blank" rel="external">这里</a>。</p>
<p>目前似乎域名还没解析，田华兄也未必决定用这个.. 所以效果的话戳<a href="http://njlug.phoenixlzx.com" target="_blank" rel="external">这里</a>看。</p>
<p>然后顺便说一句.. 现在已经健忘到我已经忘记刚刚想起来要写的东西了<em>(:з」∠)</em></p>
]]></content>
    
    
      <category term="Node.js" scheme="http://blog.phoenixlzx.com/tags/Node-js/"/>
    
      <category term="Node.js" scheme="http://blog.phoenixlzx.com/categories/Node-js/"/>
    
  </entry>
  
  <entry>
    <title><![CDATA[SubLive: 本地播放器的弹幕 API 服务器]]></title>
    <link href="http://blog.phoenixlzx.com/2014/04/28/sublive-live-subtitle-for-local-players/"/>
    <id>http://blog.phoenixlzx.com/2014/04/28/sublive-live-subtitle-for-local-players/</id>
    <published>2014-04-28T04:53:55.000Z</published>
    <updated>2014-04-27T22:06:11.000Z</updated>
    <content type="html"><![CDATA[<p>昨晚 KNA 群里在讨论给本地播放器加弹幕功能。之前虽然听说过但是并未接触过所谓「弹幕池」的概念，但是星光菊苣说只要生成 SSA 字幕直接交给播放器挂上就成。并且，(虽然很想顺带吐槽 Zht 和我们争了半天最后槽点根本不在一个点子上orz) 少将也表示时效并不重要，不需要考虑播放器的字幕自动重载问题。</p>
<p>所以躺在床上的时候就考虑了下整体的架构。还是最拿手的 HTTP RESTful API，基本功能的话就是获取字幕和发送弹幕。大致的思路如下。</p>
<ul>
<li>客户端打开媒体文件后首先获得文件的 hash。有些播放器自动寻找字幕似乎是通过文件名.. 这样的话我改个名字岂不是彻底失效。不过用 hash 来做检索文件的唯一信息的话，不同压制组放出的同一部片源都会有不同的弹幕… 不过似乎也好，如果压制了字幕进去的话，有些针对字幕君的吐槽啊什么的.. 嘛于是就这样。</li>
<li>服务端通过文件 hash 值来查找有无已存在的字幕文件。如果找到则直接发送文件给客户端，如果没有则在数据库中创建该条目，并生成新的字幕文件发送给客户端。</li>
<li>客户端以约定好的数据格式发送弹幕信息。用户友好啊之类的(所见即所得什么的..) 就都是客户端的问题了.. 服务端拿到的应该是处理好的数据。</li>
<li>服务端接收到新的弹幕后将其存进数据库，并追加到已有字幕文件。这样下次其他客户端再次请求该文件的时候就会看到弹幕。</li>
</ul>
<p>…</p>
<p>剩下的就不知道了，因为上面几条出来之后我就睡着了zzzzZ</p>
<p>…</p>
<p>早上翘课写代码这种事情早就习以为常了… 更何况是(我已经不知道该如何吐槽)的老师的上机课….</p>
<p>基本架构和功能大概 3 小时搞定，之前大概花了将近 1 个小时看 <a href="http://matroska.org/technical/specs/subtitles/ssa.html" target="_blank" rel="external">SSA/ASS Subtitles</a> 以及 Express 4.x 的文档。因为 <a href="https://github.com/visionmedia/express#quick-start" target="_blank" rel="external">Express 4.x 开始命令行工具被单独分开</a>，害我半天没找到生成模板的命令… (你丫瞎改啥啊啊啊啊</p>
<p>编码过程里倒是没遇到啥坑，就是数据处理有点烦。因为没找到啥比较好的 SSA 字幕处理库，生成的时候都是手动处理，各种 <code>+ &#39;\n&#39;</code> 什么的..</p>
<p>以及，SSA/ASS 字幕的格式特性让 <code>Array.prototype.join</code> 得以利用，省掉不少麻烦。</p>
<p>于是源码在<a href="https://github.com/phoenixlzx/sublive" target="_blank" rel="external">这里</a>。在基础架构之上还可以很方便的添加大量其他功能，验证啊什么的.. 顺便我还木有做 POST 数据的检查。这个大概晚些时候会加上。</p>
<p>四月的荒废状态持续了 27 天宣告结束。嘛，挣脱五月病要感谢星光菊苣、Zht、少将以及 KNA 字幕组的所有小伙伴们 &gt;///&lt; 感谢你们～</p>
<p>以后要继续加油喵！～</p>
]]></content>
    
    
      <category term="Node.js" scheme="http://blog.phoenixlzx.com/tags/Node-js/"/>
    
      <category term="Anime" scheme="http://blog.phoenixlzx.com/tags/Anime/"/>
    
      <category term="Node.js" scheme="http://blog.phoenixlzx.com/categories/Node-js/"/>
    
  </entry>
  
  <entry>
    <title><![CDATA[为 Linux 服务器编写的 BDRip 脚本]]></title>
    <link href="http://blog.phoenixlzx.com/2014/04/07/bdrip-script-for-linux-server/"/>
    <id>http://blog.phoenixlzx.com/2014/04/07/bdrip-script-for-linux-server/</id>
    <published>2014-04-07T12:54:18.000Z</published>
    <updated>2014-04-07T05:35:58.000Z</updated>
    <content type="html"><![CDATA[<p>最近状态比较不好… 所以准备拿学校的服务器撒撒气<em>(:з」∠)</em></p>
<p>吐槽：学校个大土豪，居然用双路 E5-2620 + 16G 内存的服务器，强烈怀疑这服务器上的站加起来不如这俩CPU贵… 另外机器上只有一块 300G 的硬盘 (SAS?) 是什么情况… 以及盗版的 RHEL5 是什么情况… 以及 /opt 分了 236G，rootfs 只有 19G 是要闹哪样…</p>
<p>于是进入正题。Linux 服务器一般都不安装图形界面，更加节省资源。同时现在也能以每月数十美元的价格租到足够性能和带宽的独立服务器，更何况把任务丢给服务器自动化操作，人的时间便可以节省下来做更重要的事情。</p>
<p>脚本需要如下软件包：</p>
<ul>
<li>ffmpeg</li>
<li>mencoder</li>
<li>x264</li>
<li>mkvtoolnix</li>
</ul>
<p>学校的服务器是 RHEL5 还没给激活，没办法换了 CentOS 的源，上面的一些软件包需要从 repoforge 获得。Ubuntu/Debian 的话，一般启用专有软件仓库就可以了。</p>
<p>下面贴脚本。</p>
<figure class="highlight bash"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div><div class="line">25</div><div class="line">26</div><div class="line">27</div><div class="line">28</div><div class="line">29</div><div class="line">30</div><div class="line">31</div><div class="line">32</div><div class="line">33</div></pre></td><td class="code"><pre><div class="line"><span class="shebang">#!/bin/bash</span></div><div class="line"><span class="comment"># $1 = origin file</span></div><div class="line"><span class="comment"># $2 = output file</span></div><div class="line"><span class="comment"># $3 = quality</span></div><div class="line"><span class="comment"># $4 = threads</span></div><div class="line"></div><div class="line"><span class="comment"># The crf option controls encoding quality, and indirectly the final</span></div><div class="line"><span class="comment"># filesize. The higher the value, the more compression and thus smaller</span></div><div class="line"><span class="comment"># file size. Reduce the value and file size will go up. A value of 21</span></div><div class="line"><span class="comment"># should produce a file of approximately 4GB in size for a typical movie.</span></div><div class="line"></div><div class="line"><span class="comment"># Encode the video using x264, ignore the audio for now.</span></div><div class="line">mencoder <span class="variable">$1</span>.m2ts \</div><div class="line">-ovc x264 -x264encopts \</div><div class="line">crf=<span class="variable">$3</span>:frameref=<span class="number">3</span>:bframes=<span class="number">3</span>:direct_pred=auto:weight_b:partitions=all:<span class="number">8</span>x8dct:me=umh:mixed_refs:trellis=<span class="number">1</span>:nopsnr:nossim:subq=<span class="number">6</span>:level_idc=<span class="number">41</span>:threads=<span class="variable">$4</span> \</div><div class="line">-nosound \</div><div class="line">-of rawvideo \</div><div class="line">-o <span class="variable">$2</span>.x264</div><div class="line"></div><div class="line"><span class="comment"># Extract first sound track, normally stream 0:1 in BDs.</span></div><div class="line">ffmpeg -i <span class="variable">$1</span>.m2ts -map <span class="number">0</span>:<span class="number">1</span> -acodec flac <span class="variable">$2</span>.flac</div><div class="line"></div><div class="line"><span class="comment"># Convert raw x264 video to MP4</span></div><div class="line">ffmpeg -i <span class="variable">$2</span>.x264 -vcodec copy <span class="variable">$2</span>.mp4</div><div class="line"></div><div class="line"><span class="comment"># Finally, merge everything into a single MKV file</span></div><div class="line">mkvmerge -o <span class="variable">$2</span>.mkv <span class="variable">$2</span>.mp4 --track-name <span class="number">0</span>:Jpn <span class="variable">$2</span>.flac</div><div class="line"></div><div class="line"><span class="comment"># Uncomment if you want to clean up your working environment.</span></div><div class="line"><span class="comment"># rm $2.x264 $2.flac $2.mp4</span></div><div class="line"></div><div class="line"><span class="comment"># Tell the user we're done.</span></div><div class="line"><span class="built_in">echo</span> <span class="string">"Rip complete. Output file is <span class="variable">$2</span>.mkv"</span></div></pre></td></tr></table></figure>

<p>保存为 <code>transcode.sh</code> 并赋予可执行权限：</p>
<figure class="highlight bash"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">$ chmod +x transcode.sh</div></pre></td></tr></table></figure>

<p>脚本基本上是从下面的参考链接里抄来的，但是做了一些更改以适合我目前的需求。脚本接受 4 个参数，分别为输入文件(不含扩展名)、输出文件(不含扩展名)、质量、线程数。我在服务器上 Rip 的文件是 zyo 买的「未確認で進行形」第一卷OVA，命令如下：</p>
<figure class="highlight bash"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">$ ./transcode.sh <span class="number">00004</span> <span class="number">4</span> <span class="number">18</span> <span class="number">20</span></div></pre></td></tr></table></figure>

<p><code>transcode.sh</code> 是当前目录下的脚本文件，<code>00004</code> 代表当前目录下的 <code>00004.m2ts</code> 片源，输出文件将是 <code>4.mkv</code>，压缩质量为 <code>18</code>，编码过程使用 20 个线程(服务器一共 12 个核心 24 个线程么我就不客气啦。</p>
<p>OVA压缩大约使用了不到 20 分钟的时间，最终文件大小不到 500M。看起来效果还可以嗯…</p>
<p>参考：</p>
<ul>
<li><a href="http://wiki.gentoo.org/wiki/FFmpeg_-_Extract_Blu-Ray_Audio" target="_blank" rel="external">FFmpeg - Extract Blu-Ray Audio - Gentoo Wiki</a></li>
<li><a href="http://www.rcomputer.eu/21-zoznam/operacne-systemy/linux/157-howto-encode-a-blu-ray-rip-into-a-smaller-format-without-losing-quality" target="_blank" rel="external">HowTo: Encode a Blu-ray rip into a smaller format without losing quality</a></li>
</ul>
]]></content>
    
    
      <category term="Multimedia" scheme="http://blog.phoenixlzx.com/tags/Multimedia/"/>
    
      <category term="Anime" scheme="http://blog.phoenixlzx.com/tags/Anime/"/>
    
      <category term="FFmpeg" scheme="http://blog.phoenixlzx.com/tags/FFmpeg/"/>
    
      <category term="Notes" scheme="http://blog.phoenixlzx.com/categories/Notes/"/>
    
  </entry>
  
  <entry>
    <title><![CDATA[制作更好的 GIF: 另一种更好的办法]]></title>
    <link href="http://blog.phoenixlzx.com/2014/03/25/yet-anothor-better-gif-method/"/>
    <id>http://blog.phoenixlzx.com/2014/03/25/yet-anothor-better-gif-method/</id>
    <published>2014-03-25T03:11:46.000Z</published>
    <updated>2014-03-24T20:29:17.000Z</updated>
    <content type="html"><![CDATA[<p>之前使用 <a href="http://blog.phoenixlzx.com/2014/02/19/make-gif-with-ffmpeg/" target="_blank" rel="external">FFmpeg 制作 GIF</a> 被 zyo 吐槽 <a href="https://plus.google.com/107142103119739092775/posts/asBTCqCnP9A" target="_blank" rel="external">明显的网纹</a>，于是找到了更好的办法。</p>
<p>需要：FFmpeg 以及 ImageMagick。</p>
<p>依旧不讨论如何截取/剪裁视频文件的问题。例如我们这里要转换的视频文件为 <code>clip.mp4</code>，首先使用 ffmpeg 转换成一个个的 PNG 文件，保持纵横比，宽为320像素，帧率为20。为防止文件名混乱，先建立一个 <code>png</code> 目录。</p>
<figure class="highlight bash"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">mkdir -p png</div><div class="line">ffmpeg -i clip.mp4 -vf scale=<span class="number">320</span>:-<span class="number">1</span> -r <span class="number">20</span> png/output%<span class="number">05</span>d.png</div></pre></td></tr></table></figure>

<p>接下来使用 ImageMagick 的 <code>convert</code> 工具来把 png 连接成 gif。顺便做优化来缩小文件体积。</p>
<figure class="highlight bash"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">convert -layers Optimize png/output*.png clip.gif</div></pre></td></tr></table></figure>

<p>这样就得到了图象效果更好的 <code>clip.gif</code> 文件了。不过还有一个问题，不管是本地图象查看器还是浏览器里，这gif都像幻灯片似的。本来觉得是帧率的问题，但是就算是尝试了原帧率一样不行。<a href="http://superuser.com/questions/569924/why-is-the-gif-i-created-so-slow" target="_blank" rel="external">Google 之发现了问题…</a></p>
<p>解决方案在<a href="http://humpy77.deviantart.com/journal/Frame-Delay-Times-for-Animated-GIFs-214150546" target="_blank" rel="external">这里</a>。不使用 <code>-delay 0</code> 或者是大于 <code>6/100</code> 的值。</p>
<p>所以刚才的命令可以稍微修改下。几次测试后发现对于我的原视频(23fps)，<code>4/100</code> 最合适。</p>
<figure class="highlight bash"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">convert -layers Optimize -delay <span class="number">4</span>/<span class="number">100</span> png/output*.png clip.gif</div></pre></td></tr></table></figure>

<p>这样制作出来的 gif 效果就非常赞啦。虽然比之前多了一个步骤，但是确实是制作了更好的gif，是更好的方法。</p>
]]></content>
    
    
      <category term="FFmpeg" scheme="http://blog.phoenixlzx.com/tags/FFmpeg/"/>
    
      <category term="ImageMagick" scheme="http://blog.phoenixlzx.com/tags/ImageMagick/"/>
    
      <category term="Anime" scheme="http://blog.phoenixlzx.com/tags/Anime/"/>
    
      <category term="Multimedia" scheme="http://blog.phoenixlzx.com/tags/Multimedia/"/>
    
      <category term="Notes" scheme="http://blog.phoenixlzx.com/categories/Notes/"/>
    
  </entry>
  
  <entry>
    <title><![CDATA[MongoDB 中文分字搜索]]></title>
    <link href="http://blog.phoenixlzx.com/2014/03/22/chinese-word-split-search-with-mongodb/"/>
    <id>http://blog.phoenixlzx.com/2014/03/22/chinese-word-split-search-with-mongodb/</id>
    <published>2014-03-22T05:05:43.000Z</published>
    <updated>2014-05-28T07:06:44.000Z</updated>
    <content type="html"><![CDATA[<p>为工作室开发的 <a href="http://idoc.nuister.org" target="_blank" rel="external">iDocument</a> 文档分享平台，因为后端数据库是 MongoDB 所以没有做站内搜索，只是放了个百度站内搜索的工具。不过后来发现BAKA百度根本不收录站内链接，所以主席强烈要求做站内搜索<em>(:з」∠)</em></p>
<p>MongoDB 的分词搜索不支持中文，而精确匹配的话基本上就没啥意义了。所以考虑一下几种方法。</p>
<ul>
<li>同义词匹配，高端的玩不转，初步想法是为每个文档对象添加一个 <code>tag</code> 来做搜索关键字，例如「高等数学」的文档可以有「高数」这个tag，搜索「高等数学」和「高数」都能得到这个文档。</li>
<li>分字匹配，可能会有少许误差，但是对于程序开发和网站运营人员来说都能减轻负担，因为不需要手动维护tag了。</li>
</ul>
<p>考虑到数据库中已经有之前的一些数据 (以及主席表示上传文档实在太累了所以强烈要求我保留原数据) 所以这里选择了分字搜索方式。</p>
<p>按照喵菊给的方法，基本的步骤如下</p>
<ol>
<li>分字函数名称为 <code>split()</code>，文档标题为 <code>title</code>，搜索字段为 <code>querytext</code>。</li>
<li>存入新的文档对象时，包含一个 attribute 名为 <code>searchIndex</code>，值为 <code>split(title)</code>。也就是将 <code>title</code> 分字后得到的数组。</li>
<li>将 <code>searchIndex</code> 编入索引。<code>ensureIndex({searchIndex: 1})</code>，顺序的话，喵菊之前提到过，但是这里暂不考虑。</li>
<li>搜索时把 <code>split(querytext)</code> 交给数据库来 <code>find()</code></li>
</ol>
<p>由于 Javascript 自带一个超级好用的 <code>String.prototype.split()</code> 方法，所以分字就是 <code>title.split(&#39;&#39;)</code>。</p>
<p>以及，喵菊说如果用数组的话就不能用 <code>/variable/i</code> 这样的正则来忽略大小写了。所以存进去新index的时候要全部转换成小写，搜索的时候也要转换成小写来实现 Case-insensitive search.</p>
<p>修改路由函数，添加一个新的属性 <code>searchIndex</code>：</p>
<figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">var</span> newdoc = {</div><div class="line">    title: req.body.title,</div><div class="line">    updateTime: <span class="built_in">Math</span>.round((<span class="keyword">new</span> <span class="built_in">Date</span>()).getTime() / <span class="number">1000</span>),</div><div class="line">    fileType: req.body.fileType,</div><div class="line">    belongs: req.body.belongs,</div><div class="line">    course: req.body.courseId,</div><div class="line">    type: req.body.type,</div><div class="line">    link: req.body.link,</div><div class="line">    downloads: <span class="number">0</span>,</div><div class="line">    searchIndex: req.body.title.toLowerCase().split(<span class="string">''</span>).clean(<span class="string">" "</span>) <span class="comment">// Remove space element after split to single text.</span></div><div class="line">};</div><div class="line"></div><div class="line"><span class="built_in">Array</span>.prototype.clean = <span class="function"><span class="keyword">function</span> <span class="params">(deleteValue)</span> </span>{</div><div class="line">    <span class="keyword">for</span> (<span class="keyword">var</span> i = <span class="number">0</span>; i &lt; <span class="keyword">this</span>.length; i++) {</div><div class="line">        <span class="keyword">if</span> (<span class="keyword">this</span>[i] == deleteValue) {</div><div class="line">            <span class="keyword">this</span>.splice(i, <span class="number">1</span>);</div><div class="line">            i--;</div><div class="line">        }</div><div class="line">    }</div><div class="line">    <span class="keyword">return</span> <span class="keyword">this</span>;</div><div class="line">};</div></pre></td></tr></table></figure>

<p>修改文档模型函数，添加时将 <code>searchIndex</code> 编入索引：</p>
<figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div></pre></td><td class="code"><pre><div class="line">exports.addnew = <span class="function"><span class="keyword">function</span><span class="params">(newdoc, callback)</span> </span>{</div><div class="line">    collection.insert(newdoc, {safe: <span class="literal">true</span>}, <span class="function"><span class="keyword">function</span><span class="params">(err)</span> </span>{</div><div class="line">        <span class="keyword">if</span> (err) {</div><div class="line">            <span class="keyword">return</span> callback(err);</div><div class="line">        }</div><div class="line">        collection.ensureIndex({</div><div class="line">            searchIndex: <span class="number">1</span></div><div class="line">        }, <span class="function"><span class="keyword">function</span><span class="params">(err)</span> </span>{</div><div class="line">            <span class="keyword">if</span> (err) {</div><div class="line">                <span class="keyword">return</span> callback(err);</div><div class="line">            }</div><div class="line">            callback(<span class="literal">null</span>);</div><div class="line">        });</div><div class="line">    });</div><div class="line">}</div></pre></td></tr></table></figure>

<p>新建文档搜索函数。 <strong>坑：如果直接把分好字的数组交给数据库来 find 那么一样是精确匹配的效果。这里应该是我对 MongoDB 还不够熟悉才掉坑里…</strong></p>
<p>正确的匹配方法是 <code>db.documents.find({ searchIndex: {$all: splittedTextArray} })</code>。</p>
<figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div></pre></td><td class="code"><pre><div class="line">exports.searchdoc = <span class="function"><span class="keyword">function</span><span class="params">(splittedTextArray, callback)</span> </span>{</div><div class="line">    collection.find({</div><div class="line">        searchIndex: {$all: splittedTextArray}</div><div class="line">    })</div><div class="line">        .sort({ downloads: -<span class="number">1</span> })</div><div class="line">        .toArray(<span class="function"><span class="keyword">function</span><span class="params">(err, docs)</span> </span>{</div><div class="line">            <span class="keyword">if</span> (err) {</div><div class="line">                <span class="keyword">return</span> callback(err, <span class="literal">null</span>);</div><div class="line">            }</div><div class="line">            callback(<span class="literal">null</span>, docs);</div><div class="line">        });</div><div class="line">}</div></pre></td></tr></table></figure>

<p>然后按照主席的要求，写了个脚本来更新之前的数据。方法是将原来的数据全部取出，然后遍历写回的同时添加 <code>searchIndex</code>。看了下原来有的数据大约就100多条，所以直接全部取了没考虑分段的问题嗯。</p>
<p>据喵菊说，数组处理是 MongoDB 的强项所以非常快～</p>
<p>整个项目的源码在<a href="https://github.com/DuoHuoStudio/iDocument" target="_blank" rel="external">这里</a></p>
<p>修改完毕，测试效果非常赞w 不过就是BAKA的blueed还没写搜索列表页的样式就是了… <del>据说前端苦逼</del></p>
]]></content>
    
    
      <category term="MongoDB" scheme="http://blog.phoenixlzx.com/tags/MongoDB/"/>
    
      <category term="Node.js" scheme="http://blog.phoenixlzx.com/tags/Node-js/"/>
    
      <category term="Database" scheme="http://blog.phoenixlzx.com/categories/Database/"/>
    
  </entry>
  
  <entry>
    <title><![CDATA[DNSMasq 建立自定义 DNS]]></title>
    <link href="http://blog.phoenixlzx.com/2014/03/11/setup-custom-dns-with-dnsmasq/"/>
    <id>http://blog.phoenixlzx.com/2014/03/11/setup-custom-dns-with-dnsmasq/</id>
    <published>2014-03-11T11:24:56.000Z</published>
    <updated>2014-03-17T04:55:47.000Z</updated>
    <content type="html"><![CDATA[<p>一直想写的<a href="https://github.com/phoenixlzx/micromoedns" target="_blank" rel="external">微萌DNS</a> 在今天上午准备好基本架构后却在下午遇到各种各样的坑而弃掉了… 心情有够不爽。所以把自己维护的缓存DNS解决方案写出来，想要折腾类似的东西的童鞋可以试试看。</p>
<p>所谓自定义DNS，这里有这么几个功能：</p>
<ul>
<li>反DNS污染</li>
<li>可以直接访问部分网站</li>
<li>一些特定的服务加速</li>
<li>解决一些国外DNS被X的问题</li>
</ul>
<p>接下来是搭建步骤。</p>
<h3 id="安装基本包">安装基本包</h3>
<p>需要：<code>dnsmasq</code> 和 <code>dnscrypt-proxy</code></p>
<p>前者在几乎所有的发行版里都有，直接装就行了。后者需要添加第三方源。如果是 Ubuntu 的话可以添加 <code>ppa:shnatsel/dnscrypt</code> 仓库然后安装这个包。</p>
<h3 id="DNSCrypt_配置">DNSCrypt 配置</h3>
<p>默认的话安装的 dnscrypt-proxy 是侦听于 <code>127.0.0.2</code> 的 <code>53</code> 端口，这里需要让 DNSMasq 来作为上游服务器所以需要修改配置。文件：<code>/etc/default/dnscrypt-proxy</code></p>
<p>修改 </p>
<figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line"><span class="attribute">local-address</span>=<span class="string">127.0.0.2:53</span></div></pre></td></tr></table></figure>

<p>为</p>
<figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line"><span class="attribute">local-address</span>=<span class="string">127.0.0.1:5301</span></div></pre></td></tr></table></figure>

<p>以及默认的DNS服务器是 OpenDNS —— 和其他欧美服务一样，节点遍布欧美亚洲就那么可怜的一两个节点，甚至一个节点都没就敢叫遍布全球了呢。所以这里最好能改一下，比如选择日本的OpenNIC节点。配置文件里添加(原本是有 OpenDNS 注释的，无视即可)</p>
<figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">resolver-address=<span class="number">106.186</span>.<span class="number">17.181</span><span class="symbol">:</span><span class="number">2053</span></div><div class="line">provider-name=<span class="number">2</span>.dnscrypt-cert.ns2.jp.dns.opennic.glue</div><div class="line">provider-key=<span class="number">8768</span><span class="symbol">:C3DB</span><span class="symbol">:F70A</span><span class="symbol">:FBC6</span><span class="symbol">:</span><span class="number">3</span><span class="constant">B64:</span><span class="number">8630</span><span class="symbol">:</span><span class="number">8167</span><span class="symbol">:</span><span class="number">2</span><span class="constant">FD4:</span><span class="constant">EE6F</span><span class="symbol">:E175</span><span class="symbol">:ECFD</span><span class="symbol">:</span><span class="number">46</span><span class="constant">C9:</span><span class="number">22</span><span class="constant">FC:</span><span class="number">7674</span><span class="symbol">:A1AC</span><span class="symbol">:</span><span class="number">2</span>E2A</div></pre></td></tr></table></figure>

<p>更多可用的 DNSCrypt 服务器在 <a href="http://dnscrypt.org/" target="_blank" rel="external">DNSCrypt主页</a> 可以找到。</p>
<p>修改好配置文件后保存，重启 dnscrypt 服务。</p>
<h3 id="两个好用的配置文件">两个好用的配置文件</h3>
<p>要感谢喵菊苣的功劳，我们有两个很方便好用的配置文件来阻止一些国内ISP的DNS污染，以及部分国内站点的加速。</p>
<p>配置文件地址在<a href="https://github.com/felixonmars/dnsmasq-china-list" target="_blank" rel="external">这里</a></p>
<figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line"># 为了方便日后更新，把这个仓库 clone 到本地而不是直接下载。</div><div class="line">git clone http<span class="variable">s:</span>//github.<span class="keyword">com</span>/felixonmars/dnsmasq-china-<span class="keyword">list</span>.git</div><div class="line"><span class="keyword">cd</span> dnsmasq-china-<span class="keyword">list</span></div><div class="line"># 创建到 DNSMasq 配置目录的软链接，当前该目录在 /root/dnsmasq-china-<span class="keyword">list</span> 下。</div><div class="line"><span class="keyword">ln</span> -<span class="keyword">s</span> /root/dnsmasq-china-<span class="keyword">list</span>/accelerated-domains.china.<span class="keyword">conf</span> /etc/dnsmasq.<span class="keyword">d</span>/</div><div class="line"><span class="keyword">ln</span> -<span class="keyword">s</span> /root/dnsmasq-china-<span class="keyword">list</span>/bogus-nxdomain.china.<span class="keyword">conf</span> /etc/dnsmasq.<span class="keyword">d</span>/</div></pre></td></tr></table></figure>

<h3 id="一份你懂的hosts和拿来主义">一份你懂的hosts和拿来主义</h3>
<p>hosts 神马的最喜欢了。自从有了它，妈妈再也不用担心我上不了 Google+ ~~</p>
<p>于是这玩意到处都有，找一份你觉得靠谱的，然后保存在一个方便找的目录下。~~注意：不要放在 <code>/etc/dnsmasq.d</code> 下，这里文件位置在 <code>/etc/dns/hosts</code></p>
<p>实在受不了渣渣 Android 的坑爹于是换了 iPhone 之后，Apple 软件的下载速度其实不比 Android 浪费我的时间少多少。一直以来我自己写 Apple 服务加速配置，通过各种查询 Apple 在 Chinacache 的CDN IP，然后写个简单的脚本保证2000个地址能平均分配到各种线路的IP。不过后来发现这种办法开始行不通了… 于是直接拿别人的来用好了。新建文件： <code>/etc/dnsmasq.d/apple.conf</code> 内容只有一行：</p>
<figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line"><span class="attribute">server</span>=<span class="string">/.apple.com/199.91.73.222</span></div></pre></td></tr></table></figure>

<p>在这里要感谢 V2EX 的 DNS 啦～</p>
<h3 id="调教_DNSMasq">调教 DNSMasq</h3>
<p>一切准备就绪，现在来看 DNSMasq 的配置文件—— <code>/etc/dnsmasq.conf</code></p>
<p>这里只写修改了的配置，其他的保持默认即可。</p>
<figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div></pre></td><td class="code"><pre><div class="line"><span class="comment"># 不读取 /etc/resolv.conf ，取消注释即可</span></div><div class="line">no-resolv</div><div class="line">no-poll</div><div class="line"><span class="comment"># 添加上游服务器为 DNSCrypt，如果还有其他的 server= 记得取消注释。</span></div><div class="line"><span class="variable">server=</span><span class="number">127.0</span>.<span class="number">0.1</span><span class="comment">#5301</span></div><div class="line"><span class="comment"># 在所有网卡上关闭 DHCP，用不着这个功能。如果有多个网卡那么一行一个。</span></div><div class="line"><span class="variable">no-dhcp-interface=</span>eth0</div><div class="line"><span class="comment"># 添加自定义 hosts 文件</span></div><div class="line"><span class="variable">addn-hosts=</span>/etc/dns/hosts</div></pre></td></tr></table></figure>

<p>一共就是这些。保存配置文件并重启 dnsmasq，这样就有了一台可以给所有设备使用的、具备防污染/服务加速以及自定义地址等功能的 DNS 服务器啦～</p>
]]></content>
    
    
      <category term="DNS" scheme="http://blog.phoenixlzx.com/tags/DNS/"/>
    
      <category term="Server" scheme="http://blog.phoenixlzx.com/tags/Server/"/>
    
      <category term="Notes" scheme="http://blog.phoenixlzx.com/categories/Notes/"/>
    
  </entry>
  
  <entry>
    <title><![CDATA[浏览器A到Z]]></title>
    <link href="http://blog.phoenixlzx.com/2014/03/10/browser-from-a-to-z/"/>
    <id>http://blog.phoenixlzx.com/2014/03/10/browser-from-a-to-z/</id>
    <published>2014-03-10T08:26:48.000Z</published>
    <updated>2014-03-10T00:58:52.000Z</updated>
    <content type="html"><![CDATA[<p>貌似很好玩的样子？</p>
<ul>
<li><strong>A</strong> amazon.cn</li>
<li><strong>B</strong> bbs.archlinuxcn.org</li>
<li><strong>C</strong> cp.ultrakvm.com</li>
<li><strong>D</strong> drive.google.com</li>
<li><strong>E</strong> expressjs.com</li>
<li><strong>F</strong> foundation.zurb.com</li>
<li><strong>G</strong> github.com</li>
<li><strong>H</strong> he.net</li>
<li><strong>I</strong> ituring.com.cn</li>
<li><strong>J</strong> jsfiddle.net</li>
<li><strong>K</strong> kde.org</li>
<li><strong>L</strong> lixian.xunlei.com</li>
<li><strong>M</strong> moedns.com</li>
<li><strong>N</strong> npmjs.org</li>
<li><strong>O</strong> ovh.com</li>
<li><strong>P</strong> plus.google.com</li>
<li><strong>Q</strong> qingcloud.com</li>
<li><strong>R</strong> readthedocs.org</li>
<li><strong>S</strong> stackoverflow.com</li>
<li><strong>T</strong> twitter.com</li>
<li><strong>U</strong> u2.dmhy.org</li>
<li><strong>V</strong> versaweb.com</li>
<li><strong>W</strong> wiki.archlinux.org</li>
<li><strong>X</strong> xehost.com</li>
<li><strong>Y</strong> yande.re</li>
<li><strong>Z</strong> zh.minecraftwiki.net</li>
</ul>
<p>发现各种不科学 = =… 而且这也只是我最近的记录而已吧 →_→</p>
]]></content>
    
    
      <category term="Life" scheme="http://blog.phoenixlzx.com/categories/Life/"/>
    
  </entry>
  
  <entry>
    <title><![CDATA[自动编译 DKMS 模组的内核 HOOK]]></title>
    <link href="http://blog.phoenixlzx.com/2014/02/23/kernel-hook-auto-build-dkms-modules/"/>
    <id>http://blog.phoenixlzx.com/2014/02/23/kernel-hook-auto-build-dkms-modules/</id>
    <published>2014-02-23T02:39:58.000Z</published>
    <updated>2014-03-09T18:47:27.000Z</updated>
    <content type="html"><![CDATA[<p><a href="http://en.wikipedia.org/wiki/Dynamic_Kernel_Module_Support" target="_blank" rel="external">DKMS</a> 是个好东西，像我们这些喜欢自定义内核的家伙有了 DKMS 之后就不用为各种第三方的内核模组发愁了～</p>
<p>于是也没看是不是有人做过了(基本上是有人做了吧…) 于是参照 <a href="https://aur.archlinux.org/packages/nvidia-hook/" target="_blank" rel="external">nvidia-hook</a> 做了一个 <a href="https://aur.archlinux.org/packages/autodkms/" target="_blank" rel="external">autodkms</a>。</p>
<p>按照 <a href="https://wiki.archlinux.org/index.php/Dynamic_Kernel_Module_Support" target="_blank" rel="external">ArchWiki</a>，自动编译所有模组的命令是：</p>
<figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line"><span class="title">dkms</span> autoinstall -k NEW_KERNEL_VERSION</div></pre></td></tr></table></figure>

<p>于是照葫芦画瓢写的 hook 文件 <code>/usr/lib/initcpio/install/autodkms</code></p>
<figure class="highlight bash"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="title">build</span></span> ()</div><div class="line">{</div><div class="line">  <span class="built_in">echo</span> <span class="string">"Building dkms modules for <span class="variable">${KERNELVERSION}</span> kernel..."</span></div><div class="line">  dkms autoinstall -k <span class="variable">${KERNELVERSION}</span></div><div class="line">  <span class="built_in">echo</span> <span class="string">"Build complete."</span></div><div class="line">}</div><div class="line"></div><div class="line"><span class="function"><span class="title">help</span></span> ()</div><div class="line">{</div><div class="line">cat&lt;&lt;HELPEOF</div><div class="line">    This hook rebuilds all dkms modules <span class="keyword">for</span> new kernel.</div><div class="line">HELPEOF</div><div class="line">}</div></pre></td></tr></table></figure>

<p>全部源码在<a href="https://github.com/phoenixlzx/autodkms" target="_blank" rel="external">这里</a> <del>其实一共就几行…</del></p>
<p>打包好之后在 <code>/etc/mkinitcpio.conf</code> 的 <code>HOOKS</code> 最后加上 <code>autodkms</code> 就可以在更新内核的时候重新编译所有模组啦。</p>
<p>=== 2014-03-10 更新 ===</p>
<p>在没有装对应内核的 headers 之前脚本执行会出错——</p>
<figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div></pre></td><td class="code"><pre><div class="line">Building dkms modules <span class="keyword">for</span> <span class="number">3.10</span>.<span class="number">33</span>-<span class="number">1</span>-lts kernel...</div><div class="line">Error! echo</div><div class="line">Your kernel headers <span class="keyword">for</span> kernel <span class="number">3.10</span>.<span class="number">33</span>-<span class="number">1</span>-lts cannot be found at</div><div class="line"><span class="regexp">/usr/</span>lib<span class="regexp">/modules/</span><span class="number">3.10</span>.<span class="number">33</span>-<span class="number">1</span>-lts<span class="regexp">/build or /</span>usr<span class="regexp">/lib/m</span>odules<span class="regexp">/3.10.33-1-lts/</span><span class="keyword">source</span>.</div><div class="line">Error! echo</div><div class="line">Your kernel headers <span class="keyword">for</span> kernel <span class="number">3.10</span>.<span class="number">33</span>-<span class="number">1</span>-lts cannot be found at</div><div class="line"><span class="regexp">/usr/</span>lib<span class="regexp">/modules/</span><span class="number">3.10</span>.<span class="number">33</span>-<span class="number">1</span>-lts<span class="regexp">/build or /</span>usr<span class="regexp">/lib/m</span>odules<span class="regexp">/3.10.33-1-lts/</span><span class="keyword">source</span>.</div><div class="line">Build complete.</div></pre></td></tr></table></figure>

<p>但是一般升级的时候都是先装内核再装 headers 所以脚本可以完善下：</p>
<figure class="highlight bash"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="title">build</span></span> ()</div><div class="line">{</div><div class="line">  <span class="keyword">if</span> [ ! <span class="operator">-d</span> <span class="string">"/usr/lib/modules/<span class="variable">${KERNELVERSION}</span>/build"</span> ]; <span class="keyword">then</span></div><div class="line">    <span class="built_in">echo</span> <span class="string">"You need to install or update <span class="variable">${KERNELVERSION}</span> headers before build kernel image."</span></div><div class="line">    <span class="keyword">exit</span> <span class="number">1</span></div><div class="line">  <span class="keyword">fi</span></div><div class="line">  <span class="built_in">echo</span> <span class="string">"Building dkms modules for <span class="variable">${KERNELVERSION}</span> kernel..."</span></div><div class="line">  dkms autoinstall -k <span class="variable">${KERNELVERSION}</span></div><div class="line">  <span class="built_in">echo</span> <span class="string">"Build complete."</span></div><div class="line">}</div><div class="line"></div><div class="line"><span class="function"><span class="title">help</span></span> ()</div><div class="line">{</div><div class="line">cat&lt;&lt;HELPEOF</div><div class="line">    This hook rebuilds all dkms modules <span class="keyword">for</span> new kernel.</div><div class="line">HELPEOF</div></pre></td></tr></table></figure>

]]></content>
    
    
      <category term="Linux" scheme="http://blog.phoenixlzx.com/tags/Linux/"/>
    
      <category term="Kernel" scheme="http://blog.phoenixlzx.com/tags/Kernel/"/>
    
      <category term="DKMS" scheme="http://blog.phoenixlzx.com/tags/DKMS/"/>
    
      <category term="Linux" scheme="http://blog.phoenixlzx.com/categories/Linux/"/>
    
  </entry>
  
  <entry>
    <title><![CDATA[使用 FFmpeg 制作 GIF]]></title>
    <link href="http://blog.phoenixlzx.com/2014/02/19/make-gif-with-ffmpeg/"/>
    <id>http://blog.phoenixlzx.com/2014/02/19/make-gif-with-ffmpeg/</id>
    <published>2014-02-19T06:03:05.000Z</published>
    <updated>2014-02-18T22:29:59.000Z</updated>
    <content type="html"><![CDATA[<p>貌似好久之前秋水学姐就问过如何制作 GIF，回想当时制作境界の彼方中「約束の絆」的<a href="https://plus.google.com/u/0/+PhoenixNemo/posts/RqPTgGrksLV" target="_blank" rel="external">GIF动画</a>，现在也已经把命令忘光了。所以觉得还是稍微做个笔记。</p>
<p>只需要用到 FFmpeg。绝大多数发行版都已经将它收录官方仓库，通过包管理器就可以安装。</p>
<p>假设我们需要转换的视频文件是 <code>input.ogg</code>，输出的GIF文件是 <code>output.gif</code>。这里不讨论如何截取视频中的段落(因为命令太繁<del>烦</del>琐了)等视频剪辑的问题，需要转换的视频已经经过简单处理，可以直接使用。</p>
<p>基本命令：</p>
<figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">ffmpeg -<span class="keyword">i</span> <span class="built_in">input</span>.ogg output.gif</div></pre></td></tr></table></figure>

<p>第一次制作的gif就是这么来的。然后当我试图上传到 Google+ 的时候发现…</p>
<p><strong>区区几百KB的视频片段制作出来的GIF竟然有20M之巨啊！</strong> <del>原视频是1080p 50fps的 flv</del></p>
<p>于是需要两个参数，一个是缩小分辨率一个是减少帧数。50帧确实少见，但是一般来讲 gif 有15帧左右就比较流畅了。命令</p>
<figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">ffmpeg -i input.ogg <span class="operator">-s</span> <span class="number">640</span>x320 -r <span class="number">15</span> output.gif</div></pre></td></tr></table></figure>

<p>很多人喜欢把 <code>-r</code> 选项放在前面… 按照某个早就忘记在哪的邮件列表里的说法，<code>-r</code> 放在 <em>输入文件</em> 的 <strong>后面</strong> 才是输出文件的效果。上面的命令出来的结果就是分辨率为<code>640×320</code>，帧率为15的gif了。<code>640x320</code> 中间是小写字母 <code>x</code> <del>别像某人一样特地复制了一份 <code>×</code> 进去…</del></p>
]]></content>
    
    
      <category term="FFmpeg" scheme="http://blog.phoenixlzx.com/tags/FFmpeg/"/>
    
      <category term="Anime" scheme="http://blog.phoenixlzx.com/tags/Anime/"/>
    
      <category term="Multimedia" scheme="http://blog.phoenixlzx.com/tags/Multimedia/"/>
    
      <category term="Notes" scheme="http://blog.phoenixlzx.com/categories/Notes/"/>
    
  </entry>
  
  <entry>
    <title><![CDATA[使用 Express.js 和 Formidable 上传文件]]></title>
    <link href="http://blog.phoenixlzx.com/2014/02/17/file-upload-with-expressjs-and-formidable/"/>
    <id>http://blog.phoenixlzx.com/2014/02/17/file-upload-with-expressjs-and-formidable/</id>
    <published>2014-02-17T11:49:22.000Z</published>
    <updated>2014-02-18T22:06:06.000Z</updated>
    <content type="html"><![CDATA[<p>写 <a href="/2014/02/06/directshare-a-simple-file-sharing-using-direct-http-links/">之前一篇文章</a> 时发现 Express.js 更新到 3.4.x 了，运行的时候出现警告</p>
<figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">connect</span>.multipart() will be removed in <span class="keyword">connect</span> <span class="number">3.0</span></div><div class="line">visit https:<span class="regexp">//github</span>.com/senchalabs/<span class="keyword">connect</span>/wiki/Connect-<span class="number">3.0</span> <span class="keyword">for</span> alternatives</div><div class="line"><span class="keyword">connect</span>.limit() will be removed in <span class="keyword">connect</span> <span class="number">3.0</span></div></pre></td></tr></table></figure>

<p>按照 Wiki 的指引看了下 <code>multiparty</code>，<code>connect-multiparty</code> 和 <code>formidable</code>，最终选用了 <code>formidable</code> <del>因为说明看起来很碉堡的样子</del></p>
<p>简单写了两个路由控制，按照 <a href="https://github.com/felixge/node-formidable" target="_blank" rel="external">node-formidable</a> 的示例加上了打印文件上传信息的几个语句，运行时发现程序 hang 了…</p>
<p>Google 许久无果——找到的结果基本都是要你注释掉 <code>app.use(express.bodyParser())</code>，而我已经是 express 3.4 所以早就没有 bodyParser 了。回去又看 formidable 给的例子，于是发现自己犯二了…——</p>
<figure class="highlight html"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line"><span class="tag">&lt;<span class="title">form</span> <span class="attribute">action</span>=<span class="value">"/upload"</span> <span class="attribute">enctype</span>=<span class="value">"multipart/form-data"</span> <span class="attribute">method</span>=<span class="value">"post"</span>&gt;</span></div></pre></td></tr></table></figure>

<p>真是个バカ！</p>
<p>立马给 <code>index.ejs</code> 中的 form 部分加上了 <code>enctype=&quot;multipart/form-data&quot;</code>，重试，成功。 <del>于是就这么解决了</del>(被打飞</p>
<p>主程序</p>
<figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div><div class="line">25</div><div class="line">26</div><div class="line">27</div><div class="line">28</div><div class="line">29</div><div class="line">30</div><div class="line">31</div><div class="line">32</div><div class="line">33</div><div class="line">34</div><div class="line">35</div><div class="line">36</div><div class="line">37</div><div class="line">38</div><div class="line">39</div><div class="line">40</div><div class="line">41</div><div class="line">42</div><div class="line">43</div><div class="line">44</div><div class="line">45</div><div class="line">46</div></pre></td><td class="code"><pre><div class="line"><span class="comment">/**</span></div><div class="line"> * Module dependencies.</div><div class="line"> */</div><div class="line"></div><div class="line"><span class="keyword">var</span> express = <span class="built_in">require</span>(<span class="string">'express'</span>);</div><div class="line"><span class="keyword">var</span> http = <span class="built_in">require</span>(<span class="string">'http'</span>);</div><div class="line"><span class="keyword">var</span> path = <span class="built_in">require</span>(<span class="string">'path'</span>);</div><div class="line"><span class="keyword">var</span> util = <span class="built_in">require</span>(<span class="string">'util'</span>);</div><div class="line"><span class="keyword">var</span> formidable = <span class="built_in">require</span>(<span class="string">'formidable'</span>);</div><div class="line"></div><div class="line"><span class="keyword">var</span> app = express();</div><div class="line"></div><div class="line"><span class="comment">// all environments</span></div><div class="line">app.set(<span class="string">'port'</span>, process.env.PORT || <span class="number">3000</span>);</div><div class="line">app.set(<span class="string">'views'</span>, path.join(__dirname, <span class="string">'views'</span>));</div><div class="line">app.set(<span class="string">'view engine'</span>, <span class="string">'ejs'</span>);</div><div class="line">app.use(express.favicon());</div><div class="line">app.use(express.logger(<span class="string">'dev'</span>));</div><div class="line">app.use(express.json());</div><div class="line">app.use(express.urlencoded());</div><div class="line">app.use(express.methodOverride());</div><div class="line">app.use(app.router);</div><div class="line">app.use(express.static(path.join(__dirname, <span class="string">'public'</span>)));</div><div class="line"></div><div class="line"><span class="comment">// development only</span></div><div class="line"><span class="keyword">if</span> (<span class="string">'development'</span> == app.get(<span class="string">'env'</span>)) {</div><div class="line">  app.use(express.errorHandler());</div><div class="line">}</div><div class="line"></div><div class="line">app.get(<span class="string">'/'</span>, <span class="function"><span class="keyword">function</span><span class="params">(req, res)</span> </span>{</div><div class="line">    res.render(<span class="string">'index'</span>);</div><div class="line">});</div><div class="line"></div><div class="line">app.post(<span class="string">'/upload'</span>, <span class="function"><span class="keyword">function</span><span class="params">(req, res)</span> </span>{</div><div class="line">    <span class="keyword">var</span> form = <span class="keyword">new</span> formidable.IncomingForm();</div><div class="line"></div><div class="line">    form.parse(req, <span class="function"><span class="keyword">function</span><span class="params">(err, fields, files)</span> </span>{</div><div class="line">        res.writeHead(<span class="number">200</span>, {<span class="string">'content-type'</span>: <span class="string">'text/plain'</span>});</div><div class="line">        res.write(<span class="string">'received upload:\n\n'</span>);</div><div class="line">        res.end(util.inspect({fields: fields, files: files}));</div><div class="line">    });</div><div class="line">});</div><div class="line"></div><div class="line">http.createServer(app).listen(app.get(<span class="string">'port'</span>), <span class="function"><span class="keyword">function</span><span class="params">()</span></span>{</div><div class="line">  <span class="built_in">console</span>.log(<span class="string">'Express server listening on port '</span> + app.get(<span class="string">'port'</span>));</div><div class="line">});</div></pre></td></tr></table></figure>

<p>以及一个简单的 <code>index.ejs</code></p>
<figure class="highlight html"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div></pre></td><td class="code"><pre><div class="line"><span class="doctype">&lt;!DOCTYPE html&gt;</span></div><div class="line"><span class="tag">&lt;<span class="title">html</span>&gt;</span></div><div class="line"><span class="tag">&lt;<span class="title">head</span>&gt;</span></div><div class="line">    <span class="tag">&lt;<span class="title">title</span>&gt;</span>Upload test<span class="tag">&lt;/<span class="title">title</span>&gt;</span></div><div class="line">    <span class="tag">&lt;<span class="title">link</span> <span class="attribute">rel</span>=<span class="value">'stylesheet'</span> <span class="attribute">href</span>=<span class="value">'/stylesheets/style.css'</span> /&gt;</span></div><div class="line"><span class="tag">&lt;/<span class="title">head</span>&gt;</span></div><div class="line"><span class="tag">&lt;<span class="title">body</span>&gt;</span></div><div class="line"><span class="tag">&lt;<span class="title">h1</span>&gt;</span>Test formidable<span class="tag">&lt;/<span class="title">h1</span>&gt;</span></div><div class="line"></div><div class="line"><span class="tag">&lt;<span class="title">form</span> <span class="attribute">action</span>=<span class="value">"/upload"</span> <span class="attribute">method</span>=<span class="value">"post"</span> <span class="attribute">enctype</span>=<span class="value">"multipart/form-data"</span>&gt;</span></div><div class="line">    <span class="tag">&lt;<span class="title">input</span> <span class="attribute">type</span>=<span class="value">"text"</span> <span class="attribute">name</span>=<span class="value">"textfield"</span>/&gt;</span></div><div class="line">    <span class="tag">&lt;<span class="title">input</span> <span class="attribute">type</span>=<span class="value">"file"</span> <span class="attribute">name</span>=<span class="value">"file"</span> /&gt;</span></div><div class="line">    <span class="tag">&lt;<span class="title">button</span> <span class="attribute">type</span>=<span class="value">"submit"</span>&gt;</span>Upload<span class="tag">&lt;/<span class="title">button</span>&gt;</span></div><div class="line"><span class="tag">&lt;/<span class="title">form</span>&gt;</span></div><div class="line"><span class="tag">&lt;/<span class="title">body</span>&gt;</span></div><div class="line"><span class="tag">&lt;/<span class="title">html</span>&gt;</span></div></pre></td></tr></table></figure>

<p>PS. <a href="http://stackoverflow.com/a/5543851" target="_blank" rel="external">后来发现确实有人遇到这个问题了…</a></p>
]]></content>
    
    
      <category term="Node.js" scheme="http://blog.phoenixlzx.com/tags/Node-js/"/>
    
      <category term="Notes" scheme="http://blog.phoenixlzx.com/categories/Notes/"/>
    
  </entry>
  
  <entry>
    <title><![CDATA[Nyaabot: 为喵窝服务器构建的 IRC 机器人]]></title>
    <link href="http://blog.phoenixlzx.com/2014/02/16/nyaabot-bot-for-nyaacat-irc/"/>
    <id>http://blog.phoenixlzx.com/2014/02/16/nyaabot-bot-for-nyaacat-irc/</id>
    <published>2014-02-16T11:19:34.000Z</published>
    <updated>2014-02-16T17:30:03.000Z</updated>
    <content type="html"><![CDATA[<p>第一次写 IRC 机器人。之前已经看到仙子酱等等菊苣们写了好多种 IRC 机器人，但是都是 Python 写的。倒不是歧视 Python 啊什么的… 主要是咱服务器上木有方便的环境跑，而且也有需要自己做的功能。正好在逛 NPM 的时候发现了 <a href="https://npmjs.org/node-irc" target="_blank" rel="external">node-irc</a> 这个包，于是折腾一下午搞出来一个机器人程序。</p>
<p>目前主要纠结的问题是 IRC 没有类似 HTTP Status code 或者类似的指示代码，所以我能想到的处理办法就是验证字符串。好吧我承认这种方法很不靠谱，但是… 各种求靠谱方法啊…</p>
<h3 id="客户端配置">客户端配置</h3>
<p>专门写了一个 <code>config.js</code> 来保存客户端的配置。根据 <a href="https://node-irc.readthedocs.org/en/latest/index.html" target="_blank" rel="external">node-irc 的文档</a>，在程序中做如下参数设定。</p>
<figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div></pre></td><td class="code"><pre><div class="line">serveroptions: {</div><div class="line">    userName: <span class="string">'nyaabot'</span>,</div><div class="line">    realName: <span class="string">'Nyaacat IRC Bot'</span>,</div><div class="line">    port: <span class="number">7000</span>,</div><div class="line">    debug: <span class="literal">false</span>,</div><div class="line">    showErrors: <span class="literal">false</span>,</div><div class="line">    autoRejoin: <span class="literal">true</span>,</div><div class="line">    autoConnect: <span class="literal">true</span>,</div><div class="line">    channels: [<span class="string">'#nyaacat'</span>],</div><div class="line">    secure: <span class="literal">true</span>,</div><div class="line">    selfSigned: <span class="literal">false</span>,</div><div class="line">    certExpired: <span class="literal">false</span>,</div><div class="line">    floodProtection: <span class="literal">true</span>,</div><div class="line">    floodProtectionDelay: <span class="number">1000</span>,</div><div class="line">    sasl: <span class="literal">false</span>,</div><div class="line">    stripColors: <span class="literal">true</span>,</div><div class="line">    channelPrefixes: <span class="string">"&#"</span>,</div><div class="line">    messageSplit: <span class="number">512</span></div><div class="line">}</div></pre></td></tr></table></figure>

<p>看起来都是很好懂的嗯。昨天在配置 CraftIRC 插件的时候撞到一个坑，NickServ 一直报错说参数不完整。调了半天发现是这里的 <code>realName</code> 部分没有填写，于是这次特地写上了注释要求该项必须填写。</p>
<h3 id="帐户验证">帐户验证</h3>
<p>之后发现木有类似验证帐号的部分。查了下找到了 <a href="https://github.com/martynsmith/node-irc/issues/205" target="_blank" rel="external">issue #205</a>。原来验证并没有在 IRC RFC 里规定，因此没有包含在库里。嘛好吧，只好自己写了。</p>
<figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div></pre></td><td class="code"><pre><div class="line">nyaa.addListener(<span class="string">"notice"</span>, <span class="function"><span class="keyword">function</span> <span class="params">(from, to, text, message)</span> </span>{</div><div class="line">    <span class="comment">// console.log(from + '\n' + to + '\n' + text + '\n' + util.inspect(message, false, null));</span></div><div class="line">    <span class="keyword">if</span> (from === <span class="string">'NickServ'</span></div><div class="line">        && to === config.serveroptions.userName</div><div class="line">        && text === <span class="string">'This nickname is registered. Please choose a different nickname, or identify via /msg NickServ identify &lt;password&gt;.'</span>) {</div><div class="line">        nyaa.say(<span class="string">'NickServ'</span>, <span class="string">'identify '</span> + config.password);</div><div class="line">    }</div><div class="line">    <span class="keyword">if</span> (from === <span class="string">'NickServ'</span></div><div class="line">        && to === config.serveroptions.userName</div><div class="line">        && text === <span class="string">'You are now identified for '</span> + config.serveroptions.userName) {</div><div class="line">        <span class="built_in">console</span>.log(<span class="string">'Login success.'</span>);</div><div class="line">    } <span class="keyword">else</span> <span class="keyword">if</span> (from === <span class="string">'NickServ'</span></div><div class="line">        && to === config.serveroptions.userName</div><div class="line">        && text === <span class="string">'Invalid password for '</span> + config.serveroptions.userName) {</div><div class="line">        <span class="built_in">console</span>.log(<span class="string">'Incorrect password. check you config!'</span>);</div><div class="line">    }</div><div class="line">});</div></pre></td></tr></table></figure>

<p>嗯现在就是验证字符串… 所以似乎只能在 freenode 上跑，其他 IRC 服务器是不是这样就不知道了。测试了下表示没问题，收到登陆成功的提示了。</p>
<h3 id="消息处理">消息处理</h3>
<p>目前不准备做私信支持，所以在收到 pm 的时候直接丢一句话过去就可以了。顺便，可以做成自定义消息的说，于是加上了 <code>messages.js</code> 包含所有的可自定义消息。</p>
<p>然后是最重要的功能之一——命令。没命令的 bot 不是好 bot 嗯。编写程序时候的想法是命令以特定字符开头，但是后面如何验证，用 Stackoverflow 上扒来的 <code>startsWith</code> 和 <code>endsWith</code> 函数似乎不能做到足够准确，所以暂且用一个字母加参数来作为命令好了。首先想到的命令功能是搜索 Wiki。毕竟 Minecraft 游戏有够复杂即便是老玩家也不一定能记住 Wiki 里的所有内容。这里想要实现的效果是，提交关键字，返回搜索到的页面链接，并发送该页面的内容总结——也就是 Mediawiki 页面的 section 0。</p>
<p>这里用到了 <code>request</code> 模块，因为 <code>http.get</code> 被弃用且 <code>http.request</code> 说实话用起来比较不爽。<code>request</code> 可以轻松得到页面内容。根据 <a href="http://en.wikipedia.org/w/api.php" target="_blank" rel="external">Mediawiki API</a> 确定使用如下2个请求参数：</p>
<ul>
<li><code>/api.php?action=query&amp;format=xml&amp;titles=[页面标题]</code></li>
<li><code>/api.php?format=json&amp;action=parse&amp;prop=text&amp;section=0&amp;page=[页面标题]</code></li>
</ul>
<p>话说我倒是想都用 JSON 的，毕竟 Javascript 原生支持的东西。但是但是第一个 API 返回的 JSON 居然把页面的 pageid 作为 key 啦！你这要我如何是好，为了拿你一个 key/value 要麻烦死了哦。所以还是选择了 XML 并且使用了 <a href="https://github.com/Leonidas-from-XIV/node-xml2js" target="_blank" rel="external">xml2js</a> 这个库。</p>
<p>第二个 API 的数据处理其实一开始也是用的 XML，因为返回的 JSON 里一个 key 是 <code>*</code>，然后我就 <del>很丢脸的</del> 不知所错了… 问了仙子才知道这种带有特殊符号的使用方法是…</p>
<figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div></pre></td><td class="code"><pre><div class="line">{</div><div class="line">    <span class="string">"v"</span>: {</div><div class="line">	      <span class="string">"*"</span>:{</div><div class="line">			<span class="string">"key1"</span>: <span class="string">"value1"</span>,</div><div class="line">			<span class="string">"key2"</span>: <span class="string">"value2"</span></div><div class="line">		  }</div><div class="line">	  }</div><div class="line">}</div></pre></td></tr></table></figure>

<p>这样的拿到 <code>*</code> 的属性值的方法是 <code>v[&#39;*&#39;]</code> ……… <del>基本功不扎实，实在是太丢脸了 &gt;////&lt;</del></p>
<p>于是拿到了返回文章 summary 的 HTML 代码。先是用正则去掉所有的 HTML Tag 和 <code>\n \r</code> 等换行符，然后发现 HTML 实体没有被转换，于是偷懒用了 <a href="https://www.npmjs.org/package/htmlstrip-native" target="_blank" rel="external">htmlstrip-native</a> 来搞定这个问题。</p>
<p>测试后发现可以正确返回得到页面的链接，但是总结发出来却是乱码。感觉应该是包含了无效的 UTF-8 编码，遂尝试使用正则 <code>/\uFFFD/g</code> 替换掉无效的编码但是不起作用。试图移除最后一个字符也是无用。折腾了大约有2个小时，突然发现如果不去掉换行符则消息发出来是正常的。但是不去掉的话一行只有几个字啊… </p>
<p>不过还是有收获，仙子根据不移除换行符则消息正常判断是 IRC 消息长度限制导致了 UTF-8 流被截断。RFC 规定 IRC 消息长度为512，但是一个 CJK 字符通常占3位。所以 512/3 差不多是170。嘛很不爽的把消息限制改成了144。这样发出来就不乱码了，但是总结大多比较长要连续发好几条消息导致游戏端被刷屏，玩家颇有微词。</p>
<p>后面就方便多了，加了一条 <code>&#39;p&#39;</code> 命令，意为 <code>play</code> 即 <del>玩坏</del>。在 <code>messages.js</code> 里设定了一个 Object 包含2个数组，对应命令消息和响应消息。程序中用 <code>async</code> 来实现命令匹配和回复，以及在没有找到可用命令时发送命令未知消息。还有一条 <code>&#39;c&#39;</code> 用来执行一些真正的命令——比如退出 <em>(:з」∠)</em></p>
<p>嘛现在这 <a href="https://github.com/phoenixlzx/nyaabot" target="_blank" rel="external">bot</a> 已经在群里开始调(wan)戏(huai)二小姐了233</p>
]]></content>
    
    
      <category term="Node.js" scheme="http://blog.phoenixlzx.com/tags/Node-js/"/>
    
      <category term="Minecraft" scheme="http://blog.phoenixlzx.com/tags/Minecraft/"/>
    
      <category term="Node.js" scheme="http://blog.phoenixlzx.com/categories/Node-js/"/>
    
  </entry>
  
  <entry>
    <title><![CDATA[一个简单的 WordPress 备份脚本]]></title>
    <link href="http://blog.phoenixlzx.com/2014/02/11/a-simple-wordpress-backup-script/"/>
    <id>http://blog.phoenixlzx.com/2014/02/11/a-simple-wordpress-backup-script/</id>
    <published>2014-02-11T12:08:56.000Z</published>
    <updated>2014-02-11T04:23:41.000Z</updated>
    <content type="html"><![CDATA[<p>居然会有人问我要这个东西… 我最不会写脚本了好吧呀！</p>
<p><del>真心觉得这货 Google 一下粗来一大坨</del></p>
<p>嘛不多说了.. 既然写了就发粗来让大家吐槽一下好了… 不光是 WordPress.. 只要是静态文件+MySQL的站都可以用。</p>
<figure class="highlight bash"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div><div class="line">25</div><div class="line">26</div><div class="line">27</div><div class="line">28</div><div class="line">29</div><div class="line">30</div><div class="line">31</div><div class="line">32</div><div class="line">33</div><div class="line">34</div><div class="line">35</div><div class="line">36</div><div class="line">37</div><div class="line">38</div><div class="line">39</div><div class="line">40</div></pre></td><td class="code"><pre><div class="line"><span class="shebang">#!/bin/bash</span></div><div class="line"><span class="comment"># settings</span></div><div class="line">mysqldbuser=<span class="string">"wordpress"</span></div><div class="line">mysqldbpass=<span class="string">"YourMySQLPassword"</span></div><div class="line">mysqldb=<span class="string">"wordpress"</span></div><div class="line">webroot=<span class="string">"/var/www/wordpress"</span></div><div class="line">backuproot=<span class="string">"/root/backup/backups"</span></div><div class="line">temproot=<span class="string">"/root/backup/temp/`date +%y-%m-%d`"</span></div><div class="line">temp=<span class="string">"/root/backup/temp"</span></div><div class="line">logfile=<span class="string">"/root/backup/backup.log"</span></div><div class="line"></div><div class="line">mkdir -p <span class="variable">$backuproot</span> <span class="variable">$temproot</span>/files</div><div class="line"></div><div class="line"><span class="comment"># backup start</span></div><div class="line"><span class="built_in">echo</span> <span class="string">"Backup start"</span> &gt;&gt; <span class="variable">$logfile</span></div><div class="line"><span class="built_in">echo</span> $(date +<span class="string">"%y-%m-%d %H:%M:%S"</span>) &gt;&gt; <span class="variable">$logfile</span> </div><div class="line"><span class="built_in">echo</span> <span class="string">"------------"</span> &gt;&gt; <span class="variable">$logfile</span></div><div class="line"></div><div class="line"><span class="comment"># database backup</span></div><div class="line"><span class="built_in">echo</span> <span class="string">"Dumping MySQL database..."</span> &gt;&gt; <span class="variable">$logfile</span></div><div class="line">mysqldump --user=<span class="variable">$mysqldbuser</span> --password=<span class="variable">$mysqldbpass</span> --databases <span class="variable">$mysqldb</span> &gt; <span class="variable">$temproot</span>/db.$(date +<span class="string">"%y-%m-%d"</span>).sql</div><div class="line"><span class="built_in">echo</span> <span class="string">"Done exporting database."</span> &gt;&gt; <span class="variable">$logfile</span></div><div class="line"></div><div class="line"><span class="comment"># copy wordpress files</span></div><div class="line"><span class="built_in">echo</span> <span class="string">"Copying website files..."</span> &gt;&gt; <span class="variable">$logfile</span></div><div class="line">cp -r <span class="variable">$webroot</span> <span class="variable">$temproot</span>/files/</div><div class="line"><span class="built_in">echo</span> <span class="string">"Done copying website files."</span> &gt;&gt; <span class="variable">$logfile</span></div><div class="line"></div><div class="line"><span class="comment"># Compress backup files</span></div><div class="line"><span class="built_in">echo</span> <span class="string">"Compressing backup files..."</span> &gt;&gt; <span class="variable">$logfile</span></div><div class="line"><span class="built_in">cd</span> <span class="variable">$temp</span></div><div class="line">tar zcf <span class="variable">$backuproot</span>/backup-$(date +<span class="string">"%y-%m-%d"</span>).tar.gz $(date +<span class="string">"%y-%m-%d"</span>) &gt;&gt; <span class="variable">$logfile</span></div><div class="line"><span class="built_in">echo</span> <span class="string">"Backup complete."</span> &gt;&gt; <span class="variable">$logfile</span></div><div class="line"></div><div class="line"><span class="comment"># Cleanup</span></div><div class="line"><span class="built_in">echo</span> <span class="string">"Cleaning up..."</span> &gt;&gt; <span class="variable">$logfile</span></div><div class="line">rm -rf <span class="variable">$temp</span>/* &gt;&gt; <span class="variable">$logfile</span></div><div class="line"></div><div class="line"><span class="built_in">echo</span> <span class="string">"------------"</span> &gt;&gt; <span class="variable">$logfile</span></div><div class="line"><span class="built_in">echo</span> &gt;&gt; <span class="variable">$logfile</span></div></pre></td></tr></table></figure>

<p>真心的没有用到任何… 高级的东西。</p>
<p>保存为文件 <code>/root/backup.sh</code> 然后修改下前面几个变量，以 root 用户新建 cronjob</p>
<p><code>crontab -e</code> 然后敲</p>
<p><code>00    01    *    *    1    /root/backup.sh</code></p>
<p>保存。这样每周一的凌晨1点(当然是你的服务器时间..) 就会自动备份到 <code>/root/backup/backups</code> 下。懒得写自动删除… 于是手动删好了。</p>
<p><del>我要是被猫菊苣笑话了某人你可死定了</del></p>
]]></content>
    
    
      <category term="WordPress" scheme="http://blog.phoenixlzx.com/tags/WordPress/"/>
    
      <category term="Script" scheme="http://blog.phoenixlzx.com/tags/Script/"/>
    
      <category term="Server" scheme="http://blog.phoenixlzx.com/tags/Server/"/>
    
      <category term="Practise" scheme="http://blog.phoenixlzx.com/categories/Practise/"/>
    
  </entry>
  
</feed>
